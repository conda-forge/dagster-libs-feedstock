# yaml-language-server: $schema=https://raw.githubusercontent.com/prefix-dev/recipe-format/main/schema.json
schema_version: 1

context:
  # source-of-truth
  version: 0.27.1
  build_number: 0
  python_max: '3.12'
  python_max_exclusive: '3.13'
  # package-specific
  # from python_modules/libraries/dagster-dbt/dagster_dbt/dbt_core_version.py
  dagster_dbt_core: dbt-core >=1.7,<1.11
  dagster_ge_python_min: '3.10'
  # derived
  patch_version: ${{ (version | split('.'))[2] }}
  dagster: dagster ==1.11.${{ patch_version }}
  dagster_python: python >=${{ python_min }},<${{ python_max_exclusive }}
  # utility
  pip_install: -m pip install . -vv --no-deps --no-build-isolation --disable-pip-version-check

recipe:
  name: dagster-libs
  # the epoch is due to a historic issue with `conda-build` outputs
  # https://github.com/conda-forge/dagster-feedstock/issues/301
  version: 1!${{ version }}

build:
  number: ${{ build_number }}

outputs:
  # A
  - package:
      name: dagster-airbyte
    source:
      url: https://pypi.org/packages/source/d/dagster-airbyte/dagster_airbyte-${{ version }}.tar.gz
      sha256: 58cb1728626788759a19c0dead26de64423b69278c02ea8ca0da12d3be0614cb
    build:
      noarch: python
      script: ${{ PYTHON }} ${{ pip_install }}
      python:
        entry_points:
          - dagster-airbyte = dagster_airbyte.cli:main
    requirements:
      host:
        - ${{ dagster_python }}
        - pip
        - python ${{ python_min }}.*
        - setuptools
      run:
        - ${{ dagster }}
        - ${{ dagster_python }}
        - python >=${{ python_min }}
        - requests
    tests:
      - python:
          pip_check: true
          python_version:
            - ${{ python_min }}.*
            - ${{ python_max }}.*
          imports:
            - dagster_airbyte
      - script: dagster-airbyte --help
    about:
      homepage: https://github.com/dagster-io/dagster/tree/master/python_modules/dagster-airbyte
      license: Apache-2.0
      license_file: LICENSE
      summary: Package for integrating Airbyte with Dagster.

  - package:
      name: dagster-aws
    source:
      url: https://pypi.org/packages/source/d/dagster-aws/dagster_aws-${{ version }}.tar.gz
      sha256: d6098fafc5a7db3cc055ab32edd5b793e362f101977db09592956ef39acd5e09
    build:
      noarch: python
      script: ${{ PYTHON }} ${{ pip_install }}
    requirements:
      host:
        - ${{ dagster_python }}
        - pip
        - python ${{ python_min }}.*
        - setuptools
      run:
        - ${{ dagster }}
        - ${{ dagster_python }}
        - boto3
        - packaging
        - python >=${{ python_min }}
        - requests
    tests:
      - python:
          pip_check: true
          python_version:
            - ${{ python_min }}.*
            - ${{ python_max }}.*
          imports:
            - dagster_aws
    about:
      homepage: https://github.com/dagster-io/dagster/tree/master/python_modules/dagster-aws
      license: Apache-2.0
      license_file: LICENSE
      summary: Package for AWS-specific Dagster framework solid and resource components.
  # B
  # C
  - package:
      name: dagster-celery
    source:
      url: https://pypi.org/packages/source/d/dagster-celery/dagster_celery-${{ version }}.tar.gz
      sha256: b8fcb84726f2ac9b856ad6e699f7bd1730fab6480b5a24b4b837132928181d1f
    build:
      noarch: python
      script: ${{ PYTHON }} ${{ pip_install }}
      python:
        entry_points:
          - dagster-celery = dagster_celery.cli:main
    requirements:
      host:
        - ${{ dagster_python }}
        - pip
        - python ${{ python_min }}.*
        - setuptools
      run:
        - ${{ dagster }}
        - ${{ dagster_python }}
        - celery >=4.3.0
        - click >=5.0,<9
        - python >=${{ python_min }}
    tests:
      - python:
          pip_check: true
          python_version:
            - ${{ python_min }}.*
            - ${{ python_max }}.*
          imports:
            - dagster_celery
      - script: dagster-celery --help

    about:
      homepage: https://github.com/dagster-io/dagster/blob/master/python_modules/dagster-celery
      license: Apache-2.0
      license_file: LICENSE
      summary: Package for using Celery as Dagster's execution engine.

  - package:
      name: dagster-celery-docker
    source:
      url: https://pypi.org/packages/source/d/dagster-celery-docker/dagster_celery_docker-${{ version }}.tar.gz
      sha256: 40b6271a536693db2c8e9c13fb905f81b4d329331a92441ce6fc115d03e35e78
    build:
      noarch: python
      script: ${{ PYTHON }} ${{ pip_install }}
    requirements:
      host:
        - ${{ dagster_python }}
        - pip
        - python ${{ python_min }}.*
        - setuptools
      run:
        - ${{ dagster }}
        - ${{ dagster_python }}
        - ${{ pin_subpackage("dagster-celery", upper_bound="x.x.x") }}
        - dagster-graphql
        - docker-py
        - python >=${{ python_min }}
    tests:
      - python:
          pip_check: true
          python_version:
            - ${{ python_min }}.*
            - ${{ python_max }}.*
          imports:
            - dagster_celery_docker
    about:
      homepage: https://github.com/dagster-io/dagster/blob/master/python_modules/dagster-celery-docker
      license: Apache-2.0
      license_file: LICENSE
      summary: A Dagster integration for celery-docker

  - package:
      name: dagster-celery-k8s
    source:
      url: https://pypi.org/packages/source/d/dagster-celery-k8s/dagster_celery_k8s-${{ version }}.tar.gz
      sha256: 8e5c1cdf83a00d052d23b0f897a120bbdeb57d7abc3056242e5f945ec9205578
    build:
      noarch: python
      script: ${{ PYTHON }} ${{ pip_install }}
    requirements:
      host:
        - ${{ dagster_python }}
        - pip
        - python ${{ python_min }}.*
        - setuptools
      run:
        - ${{ dagster }}
        - ${{ dagster_python }}
        - ${{ pin_subpackage("dagster-celery", upper_bound="x.x.x") }}
        - ${{ pin_subpackage("dagster-k8s", upper_bound="x.x.x") }}
        - python >=${{ python_min }}
    tests:
      - python:
          pip_check: true
          python_version:
            - ${{ python_min }}.*
            - ${{ python_max }}.*
          imports:
            - dagster_celery_k8s
    about:
      homepage: https://github.com/dagster-io/dagster/blob/master/python_modules/dagster-celery-k8s
      license: Apache-2.0
      license_file: LICENSE
      summary: A Dagster integration for celery-k8s-executor

  - package:
      name: dagster-census
    source:
      url: https://pypi.org/packages/source/d/dagster-census/dagster_census-${{ version }}.tar.gz
      sha256: 53869cbaeac1ad9ac21f0b57ac6c0fdfc4ecee4d5fbfd7c3abce03ee6a6329fb
    build:
      noarch: python
      script: ${{ PYTHON }} ${{ pip_install }}
    requirements:
      host:
        - ${{ dagster_python }}
        - pip
        - python ${{ python_min }}.*
        - setuptools
      run:
        - ${{ dagster }}
        - ${{ dagster_python }}
        - python >=${{ python_min }}
    tests:
      - python:
          pip_check: true
          python_version:
            - ${{ python_min }}.*
            - ${{ python_max }}.*
          imports:
            - dagster_census
    about:
      homepage: https://github.com/dagster-io/dagster/blob/master/python_modules/dagster-census
      license: Apache-2.0
      license_file: LICENSE
      summary: Package for integrating Census with Dagster.

  # D
  - package:
      name: dagster-dask
    source:
      url: https://pypi.org/packages/source/d/dagster-dask/dagster_dask-${{ version }}.tar.gz
      sha256: ecb5c00c0b1700b35c063578c9a230bec6f6d74ef5e1961b561f35fbcd60a897
    build:
      noarch: python
      script: ${{ PYTHON }} ${{ pip_install }}
    requirements:
      host:
        - ${{ dagster_python }}
        - pip
        - python ${{ python_min }}.*
        - setuptools
      run:
        - ${{ dagster }}
        - ${{ dagster_python }}
        - bokeh
        - dask >=1.2.2
        - distributed >=1.28.1
        - python >=${{ python_min }}
    tests:
      - python:
          pip_check: true
          python_version:
            - ${{ python_min }}.*
            - ${{ python_max }}.*
          imports:
            - dagster_dask
    about:
      homepage: https://github.com/dagster-io/dagster/tree/master/python_modules/dagster-dask
      license: Apache-2.0
      license_file: LICENSE
      summary: Package for using Dask as Dagster's execution engine.

  - package:
      name: dagster-datadog
    source:
      url: https://pypi.org/packages/source/d/dagster-datadog/dagster_datadog-${{ version }}.tar.gz
      sha256: c756c271d527fd21f8379e5ab9d6609df062d6f305e126bea3b75a1a2f2cb9eb
    build:
      noarch: python
      script: ${{ PYTHON }} ${{ pip_install }}
    requirements:
      host:
        - ${{ dagster_python }}
        - pip
        - python ${{ python_min }}.*
        - setuptools
      run:
        - ${{ dagster }}
        - ${{ dagster_python }}
        - datadog
        - python >=${{ python_min }}
    tests:
      - python:
          pip_check: true
          python_version:
            - ${{ python_min }}.*
            - ${{ python_max }}.*
          imports:
            - dagster_datadog
    about:
      homepage: https://github.com/dagster-io/dagster/tree/master/python_modules/libraries/dagster-datadog
      license: Apache-2.0
      license_file: LICENSE
      summary: Package for datadog Dagster framework components.

  - package:
      name: dagster-dbt
    source:
      url: https://pypi.org/packages/source/d/dagster-dbt/dagster_dbt-${{ version }}.tar.gz
      sha256: a7a7fc7199384b6f653c8ec4f069d4fb4b616e901ddf67f81c9d4f149780f090
    build:
      noarch: python
      script: ${{ PYTHON }} ${{ pip_install }}
      python:
        entry_points:
          - dagster-dbt-cloud = dagster_dbt.cloud.cli:app
          - dagster-dbt = dagster_dbt.cli.app:app
    requirements:
      host:
        - ${{ dagster_python }}
        - pip
        - python ${{ python_min }}.*
        - setuptools
      run:
        - ${{ dagster }}
        - ${{ dagster_dbt_core }}
        - ${{ dagster_python }}
        - jinja2
        - networkx
        - orjson
        - packaging
        - python >=${{ python_min }}
        - requests
        - rich
        - sqlglot-rs
        - typer >=0.9.0
    tests:
      - python:
          pip_check: true
          python_version:
            - ${{ python_min }}.*
            - ${{ python_max }}.*
          imports:
            - dagster_dbt
      - script:
          - dagster-dbt-cloud --help
          - dagster-dbt --help
    about:
      homepage: https://github.com/dagster-io/dagster/tree/master/python_modules/dagster-dbt
      license: Apache-2.0
      license_file: LICENSE
      summary: A Dagster integration for dbt

  - package:
      name: dagster-deltalake
    source:
      url: https://pypi.org/packages/source/d/dagster-deltalake/dagster_deltalake-${{ version }}.tar.gz
      sha256: 08d4dea62e8121da3929bc66d54a4806ad7933f849ebd0d74bd51b052445ac06
    build:
      noarch: python
      script: ${{ PYTHON }} ${{ pip_install }}
    requirements:
      host:
        - ${{ dagster_python }}
        - pip
        - python ${{ python_min }}.*
        - setuptools
      run:
        - ${{ dagster }}
        - ${{ dagster_python }}
        - deltalake >=0.25.0,<1
        - python >=${{ python_min }}
    tests:
      - python:
          imports: dagster_deltalake
          pip_check: true
          python_version:
            - ${{ python_min }}.*
            - ${{ python_max }}.*
    about:
      homepage: https://github.com/dagster-io/dagster/tree/master/python_modules/dagster-deltalake
      license: Apache-2.0
      license_file: LICENSE
      summary: Package for Deltalake-specific Dagster framework op and resource components.

  - package:
      name: dagster-deltalake-pandas
    source:
      url: https://pypi.org/packages/source/d/dagster-deltalake-pandas/dagster_deltalake_pandas-${{ version }}.tar.gz
      sha256: b10024647d39adedb3ab887b8fbbf3c5e6d3735331b700d875983faafa788725
    build:
      noarch: python
      script: ${{ PYTHON }} ${{ pip_install }}
    requirements:
      host:
        - ${{ dagster_python }}
        - pip
        - python ${{ python_min }}.*
        - setuptools
      run:
        - ${{ dagster }}
        - ${{ dagster_python }}
        - ${{ pin_subpackage("dagster-deltalake", upper_bound="x.x.x") }}
        - pandas
        - python >=${{ python_min }}
    tests:
      - python:
          imports: dagster_deltalake_pandas
          pip_check: true
          python_version:
            - ${{ python_min }}.*
            - ${{ python_max }}.*
    about:
      homepage: https://github.com/dagster-io/dagster/tree/master/python_modules/dagster-deltalake-pandas
      license: Apache-2.0
      license_file: LICENSE
      summary: Package for storing Pandas DataFrames in Delta tables

  - package:
      name: dagster-deltalake-polars
    source:
      url: https://pypi.org/packages/source/d/dagster-deltalake-polars/dagster_deltalake_polars-${{ version }}.tar.gz
      sha256: e78fda88f96f438b64302c755c416a4b9e61c5d34c65600bad00d5ec88b13173
    build:
      noarch: python
      script: ${{ PYTHON }} ${{ pip_install }}
    requirements:
      host:
        - ${{ dagster_python }}
        - pip
        - python ${{ python_min }}.*
        - setuptools
      run:
        - ${{ dagster }}
        - ${{ dagster_python }}
        - ${{ pin_subpackage("dagster-deltalake", upper_bound="x.x.x") }}
        - polars
        - python >=${{ python_min }}
        # TODO: update after https://github.com/conda-forge/polars-feedstock/issues/194
        # for now https://github.com/pola-rs/polars/blob/main/py-polars/pyproject.toml#L52C13-L52C29
        - pyarrow >=7.0.0
    tests:
      - python:
          imports: dagster_deltalake_polars
          pip_check: true
          python_version:
            - ${{ python_min }}.*
            - ${{ python_max }}.*
    about:
      homepage: https://github.com/dagster-io/dagster/tree/master/python_modules/dagster-deltalake-polars
      license: Apache-2.0
      license_file: LICENSE
      summary: Package for storing Polars DataFrames in Delta tables.

  - package:
      name: dagster-docker
    source:
      url: https://pypi.org/packages/source/d/dagster-docker/dagster_docker-${{ version }}.tar.gz
      sha256: b2cbe580057547a7340236eea4e3e277b4210c1ee2655e6c132663cef18968fe
    build:
      noarch: python
      script: ${{ PYTHON }} ${{ pip_install }}
    requirements:
      host:
        - ${{ dagster_python }}
        - pip
        - python ${{ python_min }}.*
        - setuptools
      run:
        - ${{ dagster }}
        - ${{ dagster_python }}
        - docker-image-py
        - docker-py
        - python >=${{ python_min }}
    tests:
      - python:
          imports: dagster_docker
          pip_check: true
          python_version:
            - ${{ python_min }}.*
            - ${{ python_max }}.*

    about:
      homepage: https://github.com/dagster-io/dagster/tree/master/python_modules/libraries/dagster-docker
      license: Apache-2.0
      license_file: LICENSE
      summary: A Dagster integration for docker


  - package:
      name: dagster-duckdb
    source:
      url: https://pypi.org/packages/source/d/dagster-duckdb/dagster_duckdb-${{ version }}.tar.gz
      sha256: 33a2e1b91eda84edd3ca8c17ffdef68ba3db637a2f3fa8ee0bd2bbea3e4b3fac
    build:
      noarch: python
      script: ${{ PYTHON }} ${{ pip_install }}
    requirements:
      host:
        - ${{ dagster_python }}
        - pip
        - python ${{ python_min }}.*
        - setuptools
      run:
        - ${{ dagster }}
        - ${{ dagster_python }}
        - python >=${{ python_min }}
        - python-duckdb
    tests:
      - python:
          imports: dagster_duckdb
          pip_check: true
          python_version:
            - ${{ python_min }}.*
            - ${{ python_max }}.*
    about:
      homepage: https://github.com/dagster-io/dagster/blob/master/python_modules/dagster-duckdb
      license: Apache-2.0
      license_file: LICENSE
      summary: Package for DuckDB-specific Dagster framework op and resource components.

  - package:
      name: dagster-duckdb-pandas
    source:
      url: https://pypi.org/packages/source/d/dagster-duckdb-pandas/dagster_duckdb_pandas-${{ version }}.tar.gz
      sha256: 62a1a6cace9e1f61ff287b30ee75431a0b66872afaefce1a2cdce8d19e6289b3
    build:
      noarch: python
      script: ${{ PYTHON }} ${{ pip_install }}
    requirements:
      host:
        - ${{ dagster_python }}
        - pip
        - python ${{ python_min }}.*
        - setuptools
      run:
        - ${{ dagster }}
        - ${{ dagster_python }}
        - ${{ pin_subpackage("dagster-duckdb", upper_bound="x.x.x") }}
        - pandas
        - python >=${{ python_min }}
    tests:
      - python:
          imports: dagster_duckdb_pandas
          pip_check: true
          python_version:
            - ${{ python_min }}.*
            - ${{ python_max }}.*
    about:
      homepage: https://github.com/dagster-io/dagster/blob/master/python_modules/dagster-duckdb-pandas
      license: Apache-2.0
      license_file: LICENSE
      summary: Package for storing Pandas DataFrames in DuckDB.

  - package:
      name: dagster-duckdb-polars
    source:
      url: https://pypi.org/packages/source/d/dagster-duckdb-polars/dagster_duckdb_polars-${{ version }}.tar.gz
      sha256: f4e9a373ef304844dfb207d2cccbc0da2a2277b24e58ed9053380df32cc2fe05
    build:
      noarch: python
      script: ${{ PYTHON }} ${{ pip_install }}
    requirements:
      host:
        - ${{ dagster_python }}
        - pip
        - python ${{ python_min }}.*
        - setuptools
      run:
        - ${{ dagster }}
        - ${{ dagster_python }}
        - ${{ pin_subpackage("dagster-duckdb", upper_bound="x.x.x") }}
        - pyarrow >=4.0.0
        - python >=${{ python_min }}
        # TODO: get polars-with-X outputs
        - polars
    tests:
      - python:
          imports: dagster_duckdb_polars
          pip_check: true
          python_version:
            - ${{ python_min }}.*
            - ${{ python_max }}.*
    about:
      homepage: https://github.com/dagster-io/dagster/blob/master/python_modules/dagster-duckdb-polars
      license: Apache-2.0
      license_file: LICENSE
      summary: Package for storing Polars DataFrames in DuckDB.
  - package:
      name: dagster-duckdb-pyspark
    source:
      url: https://pypi.org/packages/source/d/dagster-duckdb-pyspark/dagster_duckdb_pyspark-${{ version }}.tar.gz
      sha256: 6d965ab7f2fdbfe7a6769ee0f42a7af521aff1494808146709252cb1352f4c0e
    build:
      noarch: python
      script: ${{ PYTHON }} ${{ pip_install }}
    requirements:
      host:
        - ${{ dagster_python }}
        - pip
        - python ${{ python_min }}.*
        - setuptools
      run:
        - ${{ dagster }}
        - ${{ dagster_python }}
        - ${{ pin_subpackage("dagster-duckdb", upper_bound="x.x.x") }}
        - pandas
        - pyarrow
        - pyspark >=3,<4
        - python >=${{ python_min }}
    tests:
      - python:
          imports: dagster_duckdb_pyspark
          pip_check: true
          python_version:
            - ${{ python_min }}.*
            - ${{ python_max }}.*
    about:
      homepage: https://github.com/dagster-io/dagster/blob/master/python_modules/dagster-duckdb-pyspark
      license: Apache-2.0
      license_file: LICENSE
      summary: Package for storing PySpark DataFrames in DuckDB.

  # E
  # F
  - package:
      name: dagster-fivetran
    source:
      url: https://pypi.org/packages/source/d/dagster-fivetran/dagster_fivetran-${{ version }}.tar.gz
      sha256: 3e20c8b84673c286f5f088817c95ac4c52b275c15c6321b5dcb0aee10237f5bd
    build:
      noarch: python
      script: ${{ PYTHON }} ${{ pip_install }}
      python:
        entry_points:
          - dagster-fivetran = dagster_fivetran.cli:main
    requirements:
      host:
        - ${{ dagster_python }}
        - pip
        - python ${{ python_min }}.*
        - setuptools
      run:
        - ${{ dagster }}
        - ${{ dagster_python }}
        - python >=${{ python_min }}
        - requests
    tests:
      - python:
          imports: dagster_fivetran
          pip_check: true
          python_version:
            - ${{ python_min }}.*
            - ${{ python_max }}.*
      - script:
          - dagster-fivetran --help
    about:
      homepage: https://github.com/dagster-io/dagster/tree/master/python_modules/libraries/dagster-fivetran
      license: Apache-2.0
      license_file: LICENSE
      summary: Package for integrating Fivetran with Dagster.

  # G
  - package:
      name: dagster-gcp
    source:
      url: https://pypi.org/packages/source/d/dagster-gcp/dagster_gcp-${{ version }}.tar.gz
      sha256: 88d3148e8734759e235f4e447fac525a77422460bbfde75dc3ce000afce457f4
    build:
      noarch: python
      script: ${{ PYTHON }} ${{ pip_install }}
    requirements:
      host:
        - ${{ dagster_python }}
        - pip
        - python ${{ python_min }}.*
        - setuptools
      run:
        - ${{ dagster }}
        - ${{ dagster_python }}
        - ${{ pin_subpackage("dagster-pandas", upper_bound="x.x.x") }}
        - db-dtypes
        - google-api-python-client
        - google-cloud-bigquery-core >=1.28.3
        - google-cloud-storage
        - oauth2client
        - python >=${{ python_min }}
    tests:
      - python:
          imports:
            - dagster_gcp
            - dagster_gcp.bigquery
            - dagster_gcp.dataproc
            - dagster_gcp.gcs
          pip_check: true
          python_version:
            - ${{ python_min }}.*
            - ${{ python_max }}.*
    about:
      homepage: https://github.com/dagster-io/dagster/tree/master/python_modules/libraries/dagster-gcp
      license: Apache-2.0
      license_file: LICENSE
      summary: Package for GCP-specific Dagster framework solid and resource components.

  - package:
      name: dagster-gcp-pandas
    source:
      url: https://pypi.org/packages/source/d/dagster-gcp-pandas/dagster_gcp_pandas-${{ version }}.tar.gz
      sha256: f38c5e0ecb8ca5aec553e23e073568ee51b36a99074fd784aa702fad59d6631b
    build:
      noarch: python
      script: ${{ PYTHON }} ${{ pip_install }}
    requirements:
      host:
        - ${{ dagster_python }}
        - pip
        - python ${{ python_min }}.*
        - setuptools
      run:
        - ${{ dagster }}
        - ${{ dagster_python }}
        - ${{ pin_subpackage("dagster-gcp", upper_bound="x.x.x") }}
        - pandas
        - python >=${{ python_min }}
    tests:
      - python:
          imports: dagster_gcp_pandas
          pip_check: true
          python_version:
            - ${{ python_min }}.*
            - ${{ python_max }}.*
    about:
      homepage: https://github.com/dagster-io/dagster/tree/master/python_modules/libraries/dagster-gcp-pandas
      license: Apache-2.0
      license_file: LICENSE
      summary: Package for storing Pandas DataFrames in GCP.

  - package:
      name: dagster-gcp-pyspark
    source:
      url: https://pypi.org/packages/source/d/dagster-gcp-pyspark/dagster_gcp_pyspark-${{ version }}.tar.gz
      sha256: c4e88a867661c443ca0841e6f5ca8149d705bfc922bdfddaa3724669f3295247
    build:
      noarch: python
      script: ${{ PYTHON }} ${{ pip_install }}
    requirements:
      host:
        - ${{ dagster_python }}
        - pip
        - python ${{ python_min }}.*
        - setuptools
      run:
        - ${{ dagster }}
        - ${{ dagster_python }}
        - ${{ pin_subpackage("dagster-gcp", upper_bound="x.x.x") }}
        - pyspark <4
        - python >=${{ python_min }}
    tests:
      - python:
          imports: dagster_gcp_pyspark
          pip_check: true
          python_version:
            - ${{ python_min }}.*
            - ${{ python_max }}.*
    about:
      homepage: https://github.com/dagster-io/dagster/tree/master/python_modules/libraries/dagster-gcp-pyspark
      license: Apache-2.0
      license_file: LICENSE
      summary: Package for storing PySpark DataFrames in GCP

  - package:
      name: dagster-ge
    source:
      url: https://pypi.org/packages/source/d/dagster-ge/dagster_ge-${{ version }}.tar.gz
      sha256: aa07d72f160174ede39b987e3672b4368d7f028241b372d06f411f3ecd38254f
    build:
      noarch: python
      script: ${{ PYTHON }} ${{ pip_install }}
    requirements:
      host:
        - pip
        - python ${{ dagster_ge_python_min }}.*
        - setuptools
      run:
        - ${{ dagster }}
        - python >=${{ dagster_ge_python_min }},<${{ python_max_exclusive }}
        - ${{ pin_subpackage("dagster-pandas", upper_bound="x.x.x") }}
        - dataclasses
        - great-expectations >=0.17.15,<1.0.0
    tests:
      - python:
          imports: dagster_ge
          pip_check: true
          python_version:
            - ${{ dagster_ge_python_min }}.*
            - ${{ python_max }}.*
    about:
      homepage: https://github.com/dagster-io/dagster/tree/master/python_modules/libraries/dagster-ge
      license: Apache-2.0
      license_file: LICENSE
      summary: Package for GE-specific Dagster framework solid and resource components.

  - package:
      name: dagster-github
    source:
      url: https://pypi.org/packages/source/d/dagster-github/dagster_github-${{ version }}.tar.gz
      sha256: 4e863b94e050e2d690a4aa1ee0677f29efe7f21e870c30e5b8d40c922d2bb24e
    build:
      noarch: python
      script: ${{ PYTHON }} ${{ pip_install }}
    requirements:
      host:
        - ${{ dagster_python }}
        - pip
        - python ${{ python_min }}.*
        - setuptools
      run:
        - ${{ dagster }}
        - ${{ dagster_python }}
        - cryptography >=1.4
        - pyjwt
        - python >=${{ python_min }}
        - requests
    tests:
      - python:
          imports: dagster_github
          pip_check: true
          python_version:
            - ${{ python_min }}.*
            - ${{ python_max }}.*
    about:
      homepage: https://github.com/dagster-io/dagster/tree/master/python_modules/libraries/dagster-github
      license: Apache-2.0
      license_file: LICENSE
      summary: Github plugin for Dagster

  # H
  # I
  # J
  # K
  - package:
      name: dagster-k8s
    source:
      url: https://pypi.org/packages/source/d/dagster-k8s/dagster_k8s-${{ version }}.tar.gz
      sha256: fe694b0403191ff045fec123c7de6304b2cb27a208542c605788f22a412a271d
    build:
      noarch: python
      script: ${{ PYTHON }} ${{ pip_install }}
    requirements:
      host:
        - ${{ dagster_python }}
        - pip
        - python ${{ python_min }}.*
        - setuptools
      run:
        - ${{ dagster }}
        - ${{ dagster_python }}
        - google-auth !=2.23.1
        - python >=${{ python_min }}
        # from python_modules/libraries/dagster-k8s/dagster_k8s/kubernetes_version.py
        - python-kubernetes <33
    tests:
      - python:
          imports: dagster_k8s
          pip_check: true
          python_version:
            - ${{ python_min }}.*
            - ${{ python_max }}.*
    about:
      homepage: https://github.com/dagster-io/dagster/tree/master/python_modules/libraries/dagster-k8s
      license: Apache-2.0
      license_file: LICENSE
      summary: A Dagster integration for k8s

  # L
  - package:
      name: dagster-looker
    source:
      url: https://pypi.org/packages/source/d/dagster-looker/dagster_looker-${{ version }}.tar.gz
      sha256: 2c184bd55309c8e4c9eefdcff3874df80c137aff1051ec4cc5245c73bf77c885
    build:
      noarch: python
      script: ${{ PYTHON }} ${{ pip_install }}
    requirements:
      host:
        - ${{ dagster_python }}
        - pip
        - python ${{ python_min }}.*
        - setuptools
      run:
        - ${{ dagster }}
        - ${{ dagster_python }}
        - cattrs <23.2
        - lkml
        - looker-sdk <24.18.0
        - python >=${{ python_min }}
        - python-liquid <2
        - sqlglot >=25.19.0
    tests:
      - python:
          imports: dagster_looker
          pip_check: true
          python_version:
            - ${{ python_min }}.*
            - ${{ python_max }}.*
    about:
      homepage: https://github.com/dagster-io/dagster/tree/master/python_modules/libraries/dagster-looker
      license: Apache-2.0
      license_file: LICENSE
      summary: Looker with Dagster.

  # M
  - package:
      name: dagster-managed-elements
    source:
      url: https://pypi.org/packages/source/d/dagster-managed-elements/dagster_managed_elements-${{ version }}.tar.gz
      sha256: 8be1c232c81616e41ca1a8bfe4f70010627663394f804caae08a108794b82008
    build:
      noarch: python
      script: ${{ PYTHON }} ${{ pip_install }}
      python:
        entry_points:
          - dagster-managed-elements = dagster_managed_elements.cli:main
          - dagster-me = dagster_managed_elements.cli:main
    requirements:
      host:
        - ${{ dagster_python }}
        - pip
        - python ${{ python_min }}.*
        - setuptools
      run:
        - ${{ dagster }}
        - ${{ dagster_python }}
        - click-spinner
        - python >=${{ python_min }}
        - requests
    tests:
      - python:
          imports: dagster_managed_elements
          pip_check: true
          python_version:
            - ${{ python_min }}.*
            - ${{ python_max }}.*
      - script:
          - dagster-managed-elements --help
          - dagster-me --help
    about:
      homepage: https://github.com/dagster-io/dagster/tree/master/python_modules/libraries/dagster-managed-elements
      license: Apache-2.0
      license_file: LICENSE
      summary: Package for Managed elements with Dagster.

  - package:
      name: dagster-mlflow
    source:
      url: https://pypi.org/packages/source/d/dagster-mlflow/dagster_mlflow-${{ version }}.tar.gz
      sha256: 0eeb216515a13f8f9f6cf120f870bc7e4038717b2eedc4aafd5fb39be6365267
    build:
      noarch: python
      script: ${{ PYTHON }} ${{ pip_install }}
    requirements:
      host:
        - ${{ dagster_python }}
        - pip
        - python ${{ python_min }}.*
        - setuptools
      run:
        - ${{ dagster }}
        - ${{ dagster_python }}
        - mlflow
        - pandas
        - python >=${{ python_min }}
        # https://github.com/protocolbuffers/protobuf/issues/19430
        - protobuf !=5.29.0
    tests:
      - python:
          # as of #60, see `dagster-mlflow X requires mlflow, which is not installed``
          # due to some "clever" packaging tricks with mlflow(-skinny)
          # pip_check: true
          imports: dagster_mlflow
          pip_check: false
          python_version:
            - ${{ python_min }}.*
            - ${{ python_max }}.*
    about:
      homepage: https://github.com/dagster-io/dagster/tree/master/python_modules/libraries/dagster-mlflow
      license: Apache-2.0
      license_file: LICENSE
      summary: Package for mlflow Dagster framework components

  - package:
      name: dagster-msteams
    source:
      url: https://pypi.org/packages/source/d/dagster-msteams/dagster_msteams-${{ version }}.tar.gz
      sha256: 74983e547a635904051a35c91b59a7ff7dc731464a634c372f0fd8d3f1f156fd
    build:
      noarch: python
      script: ${{ PYTHON }} ${{ pip_install }}
    requirements:
      host:
        - ${{ dagster_python }}
        - pip
        - python ${{ python_min }}.*
        - setuptools
      run:
        - ${{ dagster }}
        - ${{ dagster_python }}
        - python >=${{ python_min }}
        - requests >=2,<3
    tests:
      - python:
          imports: dagster_msteams
          pip_check: true
          python_version:
            - ${{ python_min }}.*
            - ${{ python_max }}.*
    about:
      homepage: https://github.com/dagster-io/dagster/tree/master/python_modules/libraries/dagster-msteams
      license: Apache-2.0
      license_file: LICENSE
      summary: A Microsoft Teams client resource for posting to Microsoft Teams

  - package:
      name: dagster-mysql
    source:
      url: https://pypi.org/packages/source/d/dagster-mysql/dagster_mysql-${{ version }}.tar.gz
      sha256: d5bed29e54acb3ac562185d429faf87a1a71d1fe95d7ca73553494a83c65062d
    build:
      noarch: python
      script: ${{ PYTHON }} ${{ pip_install }}
    requirements:
      host:
        - ${{ dagster_python }}
        - pip
        - python ${{ python_min }}.*
        - setuptools
      run:
        - ${{ dagster }}
        - ${{ dagster_python }}
        - mysql-connector-python
        - python >=${{ python_min }}
    tests:
      - python:
          imports: dagster_mysql
          pip_check: true
          python_version:
            - ${{ python_min }}.*
            - ${{ python_max }}.*
    about:
      homepage: https://github.com/dagster-io/dagster/tree/master/python_modules/libraries/dagster-mysql
      license: Apache-2.0
      license_file: LICENSE
      summary: A Dagster integration for MySQL

  # N
  # O
  - package:
      name: dagster-openai
    source:
      url: https://pypi.org/packages/source/d/dagster-openai/dagster_openai-${{ version }}.tar.gz
      sha256: 85be6cd7df67721f5f72c544b657439be6797b028c0c91d2daceb8e594c8ccf1
    build:
      noarch: python
      script: ${{ PYTHON }} ${{ pip_install }}
    requirements:
      host:
        - ${{ dagster_python }}
        - pip
        - python ${{ python_min }}.*
        - setuptools
      run:
        - ${{ dagster }}
        - ${{ dagster_python }}
        - openai
        - python >=${{ python_min }}
    tests:
      - python:
          imports: dagster_openai
          pip_check: true
          python_version:
            - ${{ python_min }}.*
            - ${{ python_max }}.*
    about:
      homepage: https://github.com/dagster-io/dagster/tree/master/python_modules/libraries/dagster-openai
      license: Apache-2.0
      license_file: LICENSE
      summary: A Open AI client resource for interacting with Open AI API

  # P
  - package:
      name: dagster-pagerduty
    source:
      url: https://pypi.org/packages/source/d/dagster-pagerduty/dagster_pagerduty-${{ version }}.tar.gz
      sha256: bd074d98a2afc3d7d4ed8068b0fa1a0482f8ccc431a275a57daf3b4d0fe544d0
    build:
      noarch: python
      script: ${{ PYTHON }} ${{ pip_install }}
    requirements:
      host:
        - ${{ dagster_python }}
        - pip
        - python ${{ python_min }}.*
        - setuptools
      run:
        - ${{ dagster }}
        - ${{ dagster_python }}
        - pypd
        - python >=${{ python_min }}
    tests:
      - python:
          # │ │ + pip check
          # │ │ pypd 1.1.0 is not supported on this platform
          pip_check: false
          imports: dagster_pagerduty
          python_version:
            - ${{ python_min }}.*
            - ${{ python_max }}.*
    about:
      homepage: https://github.com/dagster-io/dagster/tree/master/python_modules/libraries/dagster-pagerduty
      license: Apache-2.0
      license_file: LICENSE
      summary: Package for pagerduty Dagster framework components.

  - package:
      name: dagster-pandas
    source:
      url: https://pypi.org/packages/source/d/dagster-pandas/dagster_pandas-${{ version }}.tar.gz
      sha256: d488e2de3c50e661ee4397c12b486885561006d51c8c189bf404bb4547db9c5e
    build:
      noarch: python
      script: ${{ PYTHON }} ${{ pip_install }}
    requirements:
      host:
        - ${{ dagster_python }}
        - pip
        - python ${{ python_min }}.*
        - setuptools
      run:
        - ${{ dagster }}
        - ${{ dagster_python }}
        - pandas
        - python >=${{ python_min }}
    tests:
      - python:
          imports: dagster_pandas
          pip_check: true
          python_version:
            - ${{ python_min }}.*
            - ${{ python_max }}.*
    about:
      homepage: https://github.com/dagster-io/dagster
      license: Apache-2.0
      license_file: LICENSE
      summary: Utilities and examples for working with pandas and dagster, an opinionated framework for expressing data pipelines

  - package:
      name: dagster-pandera
    source:
      url: https://pypi.org/packages/source/d/dagster-pandera/dagster_pandera-${{ version }}.tar.gz
      sha256: faa0cf68017197831fdb213ba905c033aa21053b4f885aeab31f72fdc61d2616
    build:
      noarch: python
      script: ${{ PYTHON }} ${{ pip_install }}
    requirements:
      host:
        - ${{ dagster_python }}
        - pip
        - python ${{ python_min }}.*
        - setuptools
      run:
        - ${{ dagster }}
        - ${{ dagster_python }}
        - pandera >=0.24.0
        - python >=${{ python_min }}
    tests:
      - python:
          imports: dagster_pandera
          pip_check: true
          python_version:
            - ${{ python_min }}.*
            - ${{ python_max }}.*
    about:
      homepage: https://github.com/dagster-io/dagster/tree/master/python_modules/libraries/dagster-pandera
      license: Apache-2.0
      license_file: LICENSE
      summary: Integration layer for dagster and pandera.


  - package:
      name: dagster-papertrail
    source:
      url: https://pypi.org/packages/source/d/dagster-papertrail/dagster_papertrail-${{ version }}.tar.gz
      sha256: 208a57c673e6f8100db76f5905c37a180eebad1c9a705b2e4a9d5568cc99310e
    build:
      noarch: python
      script: ${{ PYTHON }} ${{ pip_install }}
    requirements:
      host:
        - ${{ dagster_python }}
        - pip
        - python ${{ python_min }}.*
        - setuptools
      run:
        - ${{ dagster }}
        - ${{ dagster_python }}
        - python >=${{ python_min }}
    tests:
      - python:
          imports: dagster_papertrail
          pip_check: true
          python_version:
            - ${{ python_min }}.*
            - ${{ python_max }}.*
    about:
      homepage: https://github.com/dagster-io/dagster/tree/master/python_modules/libraries/dagster-papertrail
      license: Apache-2.0
      license_file: LICENSE
      summary: Package for papertrail Dagster framework components.

  - package:
      name: dagster-postgres
    source:
      url: https://pypi.org/packages/source/d/dagster-postgres/dagster_postgres-${{ version }}.tar.gz
      sha256: a00e407ef339776513f6752b25df91a517331a0872ebbfaca4c0797e3bedcd9f
    build:
      noarch: python
      script: ${{ PYTHON }} ${{ pip_install }}
    requirements:
      host:
        - ${{ dagster_python }}
        - pip
        - python ${{ python_min }}.*
        - setuptools
      run:
        - ${{ dagster }}
        - ${{ dagster_python }}
        - psycopg2-binary
        - python >=${{ python_min }}
    tests:
      - python:
          imports: dagster_postgres
          pip_check: true
          python_version:
            - ${{ python_min }}.*
            - ${{ python_max }}.*
    about:
      homepage: https://github.com/dagster-io/dagster/tree/master/python_modules/libraries/dagster-postgres
      license: Apache-2.0
      license_file: LICENSE
      summary: A Dagster integration for postgres

  - package:
      name: dagster-powerbi
    source:
      url: https://pypi.org/packages/source/d/dagster-powerbi/dagster_powerbi-${{ version }}.tar.gz
      sha256: 4a6a627c549baf7f8b7c52e0667eef32e52cd578cfa68302b092fbd46c710ab6
    build:
      noarch: python
      script: ${{ PYTHON }} ${{ pip_install }}
    requirements:
      host:
        - ${{ dagster_python }}
        - pip
        - python ${{ python_min }}.*
        - setuptools
      run:
        - ${{ dagster }}
        - ${{ dagster_python }}
        - python >=${{ python_min }}
    tests:
      - python:
          imports: dagster_powerbi
          pip_check: true
          python_version:
            - ${{ python_min }}.*
            - ${{ python_max }}.*
    about:
      homepage: https://github.com/dagster-io/dagster/tree/master/python_modules/libraries/dagster-powerbi
      license: Apache-2.0
      license_file: LICENSE
      summary: Build assets representing Power BI dashboards and reports.

  - package:
      name: dagster-prometheus
    source:
      url: https://pypi.org/packages/source/d/dagster-prometheus/dagster_prometheus-${{ version }}.tar.gz
      sha256: 4663f8a54869fb8cab42cc64d7e4e5926d60d72565b33f7a3f4dd51ef62bf989
    build:
      noarch: python
      script: ${{ PYTHON }} ${{ pip_install }}
    requirements:
      host:
        - ${{ dagster_python }}
        - pip
        - python ${{ python_min }}.*
        - setuptools
      run:
        - ${{ dagster }}
        - ${{ dagster_python }}
        - prometheus_client
        - python >=${{ python_min }}
    tests:
      - python:
          imports: dagster_prometheus
          pip_check: true
          python_version:
            - ${{ python_min }}.*
            - ${{ python_max }}.*
    about:
      homepage: https://github.com/dagster-io/dagster/tree/master/python_modules/libraries/dagster-prometheus
      license: Apache-2.0
      license_file: LICENSE
      summary: A Dagster integration for prometheus

  - package:
      name: dagster-pyspark
    source:
      url: https://pypi.org/packages/source/d/dagster-pyspark/dagster_pyspark-${{ version }}.tar.gz
      sha256: c8825a4ac487143022d6bf8484f4fa0ce69350c641755868359e176c7b884097
    build:
      noarch: python
      script: ${{ PYTHON }} ${{ pip_install }}
    requirements:
      host:
        - ${{ dagster_python }}
        - pip
        - python ${{ python_min }}.*
        - setuptools
      run:
        - ${{ dagster }}
        - ${{ dagster_python }}
        - ${{ pin_subpackage("dagster-spark", upper_bound="x.x.x") }}
        - pyspark >=3,<4
        - python >=${{ python_min }}
    tests:
      - python:
          imports: dagster_pyspark
          pip_check: true
          python_version:
            - ${{ python_min }}.*
            - ${{ python_max }}.*
    about:
      homepage: https://github.com/dagster-io/dagster/tree/master/python_modules/libraries/dagster-pyspark
      license: Apache-2.0
      license_file: LICENSE
      summary: Package for PySpark Dagster framework components.

  # Q
  # R
  # S
  - package:
      name: dagster-sigma
    source:
      url: https://pypi.org/packages/source/d/dagster-sigma/dagster_sigma-${{ version }}.tar.gz
      sha256: 0d558140ad361f1f0db9a3a7ed95bd4ea12b66e5c8d69d444fca4bffcb1acafb
    build:
      noarch: python
      script: ${{ PYTHON }} ${{ pip_install }}
      python:
        entry_points:
          - dagster-sigma = dagster_sigma.cli:app
    requirements:
      host:
        - ${{ dagster_python }}
        - pip
        - python ${{ python_min }}.*
        - setuptools
      run:
        - ${{ dagster }}
        - ${{ dagster_python }}
        - aiohttp
        - python >=${{ python_min }}
        - sqlglot
    tests:
      - python:
          imports: dagster_sigma
          pip_check: true
          python_version:
            - ${{ python_min }}.*
            - ${{ python_max }}.*
      - script:
          - which dagster-sigma || where dagster-sigma
    about:
      homepage: https://github.com/dagster-io/dagster/tree/master/python_modules/libraries/dagster-sigma
      license: Apache-2.0
      license_file: LICENSE
      summary: Build assets representing Sigma dashboards.

  - package:
      name: dagster-slack
    source:
      url: https://pypi.org/packages/source/d/dagster-slack/dagster_slack-${{ version }}.tar.gz
      sha256: 9d6a928fc29e3fa0598b4029e7734cd29ff7f48e1e4e7374bf0fd6a8ea1ebf5c
    build:
      noarch: python
      script: ${{ PYTHON }} ${{ pip_install }}
    requirements:
      host:
        - ${{ dagster_python }}
        - pip
        - python ${{ python_min }}.*
        - setuptools
      run:
        - ${{ dagster }}
        - ${{ dagster_python }}
        - python >=${{ python_min }}
        - slack-sdk
    tests:
      - python:
          imports: dagster_slack
          pip_check: true
          python_version:
            - ${{ python_min }}.*
            - ${{ python_max }}.*
    about:
      homepage: https://github.com/dagster-io/dagster/tree/master/python_modules/libraries/dagster-slack
      license: Apache-2.0
      license_file: LICENSE
      summary: A Slack client resource for posting to Slack

  - package:
      name: dagster-snowflake
    source:
      url: https://pypi.org/packages/source/d/dagster-snowflake/dagster_snowflake-${{ version }}.tar.gz
      sha256: c817039a14f1f64fc4cf69e151c490377c70ab959ecf741d6665953789d0cefc
    build:
      noarch: python
      script: ${{ PYTHON }} ${{ pip_install }}
    requirements:
      host:
        - ${{ dagster_python }}
        - pip
        - python ${{ python_min }}.*
        - setuptools
      run:
        - ${{ dagster }}
        - ${{ dagster_python }}
        - pyopenssl >=22.1.0
        - python >=${{ python_min }}
        - snowflake-connector-python >=3.4.0
    tests:
      - python:
          imports: dagster_snowflake
          pip_check: true
          python_version:
            - ${{ python_min }}.*
            - ${{ python_max }}.*
    about:
      homepage: https://github.com/dagster-io/dagster/tree/master/python_modules/libraries/dagster-snowflake
      license: Apache-2.0
      license_file: LICENSE
      summary: Package for Snowflake Dagster framework components.

  - package:
      name: dagster-spark
    source:
      url: https://pypi.org/packages/source/d/dagster-spark/dagster_spark-${{ version }}.tar.gz
      sha256: 634bdb9208c438b73f1108ac90f784cda0e7adeaa7fbd3304c991c335000879a
    build:
      noarch: python
      script: ${{ PYTHON }} ${{ pip_install }}
    requirements:
      host:
        - ${{ dagster_python }}
        - pip
        - python ${{ python_min }}.*
        - setuptools
      run:
        - ${{ dagster }}
        - ${{ dagster_python }}
        - python >=${{ python_min }}
    tests:
      - python:
          imports: dagster_spark
          pip_check: true
          python_version:
            - ${{ python_min }}.*
            - ${{ python_max }}.*
    about:
      homepage: https://github.com/dagster-io/dagster/tree/master/python_modules/libraries/dagster-spark
      license: Apache-2.0
      license_file: LICENSE
      summary: Package for Spark Dagster framework components.

  - package:
      name: dagster-snowflake-pandas
    source:
      url: https://pypi.org/packages/source/d/dagster-snowflake-pandas/dagster_snowflake_pandas-${{ version }}.tar.gz
      sha256: 09e3889803a9093dbc1bd9749f8d805d9292e7c22bdd3f640627f17718052704
    build:
      noarch: python
      script: ${{ PYTHON }} ${{ pip_install }}
    requirements:
      host:
        - ${{ dagster_python }}
        - pip
        - python ${{ python_min }}.*
        - setuptools
      run:
        - ${{ dagster }}
        - ${{ dagster_python }}
        - ${{ pin_subpackage("dagster-snowflake", upper_bound="x.x.x") }}
        - pandas
        - python >=${{ python_min }}
        - requests
        - snowflake-connector-python >=2.1.0
        - snowflake-sqlalchemy >=1.2
        - sqlalchemy !=1.4.42
    tests:
      - python:
          imports: dagster_snowflake_pandas
          pip_check: true
          python_version:
            - ${{ python_min }}.*
            - ${{ python_max }}.*
    about:
      homepage: https://github.com/dagster-io/dagster/tree/master/python_modules/libraries/dagster-snowflake-pandas
      license: Apache-2.0
      license_file: LICENSE
      summary: Package for integrating Snowflake and Pandas with Dagster.

  - package:
      name: dagster-snowflake-pyspark
    source:
      url: https://pypi.org/packages/source/d/dagster-snowflake-pyspark/dagster_snowflake_pyspark-${{ version }}.tar.gz
      sha256: bb7674c7a2792c07b43f13fa417419d9934984702cdf8cb50ed9f290470ddeba
    build:
      noarch: python
      script: ${{ PYTHON }} ${{ pip_install }}
    requirements:
      host:
        - ${{ dagster_python }}
        - pip
        - python ${{ python_min }}.*
        - setuptools
      run:
        - ${{ dagster }}
        - ${{ dagster_python }}
        - ${{ pin_subpackage("dagster-snowflake", upper_bound="x.x.x") }}
        - pyspark <4
        - python >=${{ python_min }}
        - requests
        - snowflake-sqlalchemy
    tests:
      - python:
          imports: dagster_snowflake_pyspark
          pip_check: true
          python_version:
            - ${{ python_min }}.*
            - ${{ python_max }}.*
    about:
      homepage: https://github.com/dagster-io/dagster/tree/master/python_modules/libraries/dagster-snowflake-pyspark
      license: Apache-2.0
      license_file: LICENSE
      summary: Package for integrating Snowflake and PySpark with Dagster.

  - package:
      name: dagster-ssh
    source:
      url: https://pypi.org/packages/source/d/dagster-ssh/dagster_ssh-${{ version }}.tar.gz
      sha256: 37ec662a99a1be04904583e9cdbd1c13478d5135cf7c0804d1541055b1d99208
    build:
      noarch: python
      script: ${{ PYTHON }} ${{ pip_install }}
    requirements:
      host:
        - ${{ dagster_python }}
        - pip
        - python ${{ python_min }}.*
        - setuptools
      run:
        - ${{ dagster }}
        - ${{ dagster_python }}
        - paramiko
        - python >=${{ python_min }}
        - sshtunnel
    tests:
      - python:
          imports: dagster_ssh
          pip_check: true
          python_version:
            - ${{ python_min }}.*
            - ${{ python_max }}.*
    about:
      homepage: https://github.com/dagster-io/dagster/tree/master/python_modules/libraries/dagster-ssh
      license: Apache-2.0
      license_file: LICENSE
      summary: Package for ssh Dagster framework components.

  # T
  - package:
      name: dagster-tableau
    source:
      url: https://pypi.org/packages/source/d/dagster-tableau/dagster_tableau-${{ version }}.tar.gz
      sha256: ef22ba8275f9429c7ec799dee1158172a7499c916ba1367183978bd0e73556a2
    build:
      noarch: python
      script: ${{ PYTHON }} ${{ pip_install }}
    requirements:
      host:
        - ${{ dagster_python }}
        - pip
        - python ${{ python_min }}.*
        - setuptools
      run:
        - ${{ dagster }}
        - ${{ dagster_python }}
        - pyjwt
        - python >=${{ python_min }}
        - tableauserverclient >=0.32
    tests:
      - python:
          imports: dagster_tableau
          pip_check: true
          python_version:
            - ${{ python_min }}.*
            - ${{ python_max }}.*
    about:
      homepage: https://github.com/dagster-io/dagster/tree/master/python_modules/libraries/dagster-tableau
      license: Apache-2.0
      license_file: LICENSE
      summary: Build assets representing Tableau workbooks and sheets.

  - package:
      name: dagster-twilio
    source:
      url: https://pypi.org/packages/source/d/dagster-twilio/dagster_twilio-${{ version }}.tar.gz
      sha256: a6ab5e3e260e359328986db4c3be336fc8b1756d650a0c9a16b8af7a2ff2a064
    build:
      noarch: python
      script: ${{ PYTHON }} ${{ pip_install }}
    requirements:
      host:
        - ${{ dagster_python }}
        - pip
        - python ${{ python_min }}.*
        - setuptools
      run:
        - ${{ dagster }}
        - ${{ dagster_python }}
        - python >=${{ python_min }}
        - twilio
    tests:
      - python:
          imports: dagster_twilio
          pip_check: true
          python_version:
            - ${{ python_min }}.*
            - ${{ python_max }}.*
    about:
      homepage: https://github.com/dagster-io/dagster/tree/master/python_modules/libraries/dagster-twilio
      license: Apache-2.0
      license_file: LICENSE
      summary: A Dagster integration for twilio

  # U
  # V
  # W
  - package:
      name: dagster-wandb
    source:
      url: https://pypi.org/packages/source/d/dagster-wandb/dagster_wandb-${{ version }}.tar.gz
      sha256: b3f9ebd93b85fd288ce723f835a77ee40a33a14e6b67844d177f8a0e64e2b071
    build:
      noarch: python
      script: ${{ PYTHON }} ${{ pip_install }}
    requirements:
      host:
        - ${{ dagster_python }}
        - pip
        - python ${{ python_min }}.*
        - setuptools
      run:
        - ${{ dagster }}
        - ${{ dagster_python }}
        - python >=${{ python_min }}
        - wandb >=0.15.11,<1.0
    tests:
      - python:
          imports: dagster_wandb
          pip_check: true
          python_version:
            - ${{ python_min }}.*
            - ${{ python_max }}.*
    about:
      homepage: https://github.com/dagster-io/dagster/tree/master/python_modules/dagster-wandb
      license: Apache-2.0
      license_file: LICENSE
      summary: Package for wandb Dagster components.

  # X
  # Y
  # Z

  # not hyphenated

  - package:
      name: dagstermill
    source:
      url: https://pypi.org/packages/source/d/dagstermill/dagstermill-${{ version }}.tar.gz
      sha256: e2e7702e5ecb10f0cbcd405bad05b82346c54f1afde55b616bfb0d401f2733f3
    build:
      noarch: python
      script: ${{ PYTHON }} ${{ pip_install }}
      python:
        entry_points:
          - dagstermill = dagstermill.cli:main
    requirements:
      host:
        - ${{ dagster_python }}
        - pip
        - python ${{ python_min }}.*
        - setuptools
      run:
        - ${{ dagster }}
        - ${{ dagster_python }}
        - ipykernel >=4.9.0,!=5.4.0,!=5.4.1
        - ipython_genutils >=0.2.0
        - jupyter_client <8
        - nbconvert
        - packaging >=20.9
        - papermill >=1.0.0
        - python >=${{ python_min }}
        - scrapbook >=0.5
    tests:
      - python:
          imports: dagstermill
          pip_check: true
          python_version:
            - ${{ python_min }}.*
            - ${{ python_max }}.*
      - script:
          - dagstermill --help
    about:
      homepage: https://github.com/dagster-io/dagster/tree/master/python_modules/dagstermill
      license: Apache-2.0
      license_file: LICENSE
      summary: A Dagster integration for papermill

about:
  homepage: https://github.com/dagster-io
  summary: The data orchestration platform built for productivity.
  license: Apache-2.0
  license_file: LICENSE
  documentation: https://docs.dagster.io
  description: >-
    Dagster is a system for building modern data applications. Combining an elegant programming model and beautiful tools, Dagster allows infrastructure engineers, data engineers, and data scientists to seamlessly collaborate to process and produce the trusted, reliable data needed in today's world.

extra:
  feedstock-name: dagster-libs
  recipe-maintainers:
    - bollwyvl
    - conda-forge/dagster
