# yaml-language-server: $schema=https://raw.githubusercontent.com/prefix-dev/recipe-format/main/schema.json
schema_version: 1

context:
  version: 0.26.10
  python_min: ${{ python_min | default("3.9") }}
  patch_version: ${{ (version | split('.'))[2] }}
  dagster: dagster ==1.10.${{ patch_version }}
  dagster_python: python >=${{ python_min }},<3.13
  dagster_ge_python: python >=3.10,<3.13
  # TODO: fix upstream
  # kombu 5.4.1 has requirement typing-extensions==4.12.2; python_version < "3.10", but you have typing-extensions 4.13.0.
  # python_version: ${{ python_min }}
  dagster_celery_python_test_min: '3.10'
  # from python_modules/libraries/dagster-dbt/dagster_dbt/dbt_core_version.py
  dagster_dbt_core: dbt-core >=1.7,<1.10
  # utility
  pip_install: -m pip install . -vv --no-deps --no-build-isolation --disable-pip-version-check

recipe:
  name: dagster-libs
  # the epoch is due to a historic issue with `conda-build` outputs
  # https://github.com/conda-forge/dagster-feedstock/issues/301
  version: 1!${{ version }}

build:
  number: 0

outputs:
  # A
  - package:
      name: dagster-airbyte
    source:
      url: https://pypi.org/packages/source/d/dagster-airbyte/dagster-airbyte-${{ version }}.tar.gz
      sha256: 3717f7c09a3b01e55b0d3d2b4e8b1281d74891abc1ec7361aba99a9a498f0909
    build:
      noarch: python
      script: ${{ PYTHON }} ${{ pip_install }}
      python:
        entry_points:
          - dagster-airbyte = dagster_airbyte.cli:main
    requirements:
      host:
        - ${{ dagster_python }}
        - pip
        - python ${{ python_min }}.*
        - setuptools
      run:
        - ${{ dagster }}
        - ${{ dagster_python }}
        - python >=${{ python_min }}
        - requests
    tests:
      - python:
          pip_check: true
          python_version: ${{ python_min }}
          imports:
            - dagster_airbyte
      - script: dagster-airbyte --help
    about:
      homepage: https://github.com/dagster-io/dagster/tree/master/python_modules/dagster-airbyte
      license: Apache-2.0
      license_file: LICENSE
      summary: Package for integrating Airbyte with Dagster.

  - package:
      name: dagster-aws
    source:
      url: https://pypi.org/packages/source/d/dagster-aws/dagster-aws-${{ version }}.tar.gz
      sha256: 75cc6e8694b33b0f33fbb9a9cd6b25234afbf7b77ba44d00c8116fa1baf97633
    build:
      noarch: python
      script: ${{ PYTHON }} ${{ pip_install }}
    requirements:
      host:
        - ${{ dagster_python }}
        - pip
        - python ${{ python_min }}.*
        - setuptools
      run:
        - ${{ dagster }}
        - ${{ dagster_python }}
        - boto3
        - packaging
        - python >=${{ python_min }}
        - requests
    tests:
      - python:
          pip_check: true
          python_version: ${{ python_min }}
          imports:
            - dagster_aws
    about:
      homepage: https://github.com/dagster-io/dagster/tree/master/python_modules/dagster-aws
      license: Apache-2.0
      license_file: LICENSE
      summary: Package for AWS-specific Dagster framework solid and resource components.
  # B
  # C
  - package:
      name: dagster-celery
    source:
      url: https://pypi.org/packages/source/d/dagster-celery/dagster-celery-${{ version }}.tar.gz
      sha256: c0c9b1c25a9475ac6a1dbaf22883f2276db46fc3fa0ab800260ae24df04df80f
    build:
      noarch: python
      script: ${{ PYTHON }} ${{ pip_install }}
      python:
        entry_points:
          - dagster-celery = dagster_celery.cli:main
    requirements:
      host:
        - ${{ dagster_python }}
        - pip
        - python ${{ python_min }}.*
        - setuptools
      run:
        - ${{ dagster }}
        - ${{ dagster_python }}
        - celery >=4.3.0
        - click >=5.0,<9
        - python >=${{ python_min }}
    tests:
      - python:
          pip_check: true
          python_version: ${{ dagster_celery_python_test_min }}.*
          imports:
            - dagster_celery
      - script: dagster-celery --help

    about:
      homepage: https://github.com/dagster-io/dagster/blob/master/python_modules/dagster-celery
      license: Apache-2.0
      license_file: LICENSE
      summary: Package for using Celery as Dagster's execution engine.

  - package:
      name: dagster-celery-docker
    source:
      url: https://pypi.org/packages/source/d/dagster-celery-docker/dagster-celery-docker-${{ version }}.tar.gz
      sha256: 15750011823ca47ef2867613ecb15b5382e5ae7fff086d79dd3e5a925af93487
    build:
      noarch: python
      script: ${{ PYTHON }} ${{ pip_install }}
    requirements:
      host:
        - ${{ dagster_python }}
        - pip
        - python ${{ python_min }}.*
        - setuptools
      run:
        - ${{ dagster }}
        - ${{ dagster_python }}
        - ${{ pin_subpackage("dagster-celery", upper_bound="x.x.x") }}
        - dagster-graphql
        - docker-py
        - python >=${{ python_min }}
    tests:
      - python:
          pip_check: true
          python_version: ${{ dagster_celery_python_test_min }}.*
          imports:
            - dagster_celery_docker
    about:
      homepage: https://github.com/dagster-io/dagster/blob/master/python_modules/dagster-celery-docker
      license: Apache-2.0
      license_file: LICENSE
      summary: A Dagster integration for celery-docker

  - package:
      name: dagster-celery-k8s
    source:
      url: https://pypi.org/packages/source/d/dagster-celery-k8s/dagster-celery-k8s-${{ version }}.tar.gz
      sha256: d3a6c24bd5e81b53d01e20dbc483535c8420751b1bf889a62785b36054694868
    build:
      noarch: python
      script: ${{ PYTHON }} ${{ pip_install }}
    requirements:
      host:
        - ${{ dagster_python }}
        - pip
        - python ${{ python_min }}.*
        - setuptools
      run:
        - ${{ dagster }}
        - ${{ dagster_python }}
        - ${{ pin_subpackage("dagster-celery", upper_bound="x.x.x") }}
        - ${{ pin_subpackage("dagster-k8s", upper_bound="x.x.x") }}
        - python >=${{ python_min }}
    tests:
      - python:
          pip_check: true
          python_version: ${{ dagster_celery_python_test_min }}.*
          imports:
            - dagster_celery_k8s
    about:
      homepage: https://github.com/dagster-io/dagster/blob/master/python_modules/dagster-celery-k8s
      license: Apache-2.0
      license_file: LICENSE
      summary: A Dagster integration for celery-k8s-executor

  - package:
      name: dagster-census
    source:
      url: https://pypi.org/packages/source/d/dagster-census/dagster-census-${{ version }}.tar.gz
      sha256: e0563384fa796f8d630f4972cefff5a505db0d219137cd26cb7d05b38c895dbe
    build:
      noarch: python
      script: ${{ PYTHON }} ${{ pip_install }}
    requirements:
      host:
        - ${{ dagster_python }}
        - pip
        - python ${{ python_min }}.*
        - setuptools
      run:
        - ${{ dagster }}
        - ${{ dagster_python }}
        - python >=${{ python_min }}
    tests:
      - python:
          pip_check: true
          python_version: ${{ python_min }}
          imports:
            - dagster_census
    about:
      homepage: https://github.com/dagster-io/dagster/blob/master/python_modules/dagster-census
      license: Apache-2.0
      license_file: LICENSE
      summary: Package for integrating Census with Dagster.

  # D
  - package:
      name: dagster-dask
    source:
      url: https://pypi.org/packages/source/d/dagster-dask/dagster-dask-${{ version }}.tar.gz
      sha256: abc2ca1071134ade6a1170a1dcf298555e35de6e81471950f142397cff680151
    build:
      noarch: python
      script: ${{ PYTHON }} ${{ pip_install }}
    requirements:
      host:
        - ${{ dagster_python }}
        - pip
        - python ${{ python_min }}.*
        - setuptools
      run:
        - ${{ dagster }}
        - ${{ dagster_python }}
        - bokeh
        - dask >=1.2.2
        - distributed >=1.28.1
        - python >=${{ python_min }}
    tests:
      - python:
          pip_check: true
          python_version: ${{ python_min }}
          imports:
            - dagster_dask
    about:
      homepage: https://github.com/dagster-io/dagster/tree/master/python_modules/dagster-dask
      license: Apache-2.0
      license_file: LICENSE
      summary: Package for using Dask as Dagster's execution engine.

  - package:
      name: dagster-datadog
    source:
      url: https://pypi.org/packages/source/d/dagster-datadog/dagster-datadog-${{ version }}.tar.gz
      sha256: e0a217b5a02339a42c3f9c5373aab88d6887cc1feccddd56dcebf9b9d56ec411
    build:
      noarch: python
      script: ${{ PYTHON }} ${{ pip_install }}
    requirements:
      host:
        - ${{ dagster_python }}
        - pip
        - python ${{ python_min }}.*
        - setuptools
      run:
        - ${{ dagster }}
        - ${{ dagster_python }}
        - datadog
        - python >=${{ python_min }}
    tests:
      - python:
          pip_check: true
          python_version: ${{ python_min }}
          imports:
            - dagster_datadog
    about:
      homepage: https://github.com/dagster-io/dagster/tree/master/python_modules/libraries/dagster-datadog
      license: Apache-2.0
      license_file: LICENSE
      summary: Package for datadog Dagster framework components.

  - package:
      name: dagster-dbt
    source:
      url: https://pypi.org/packages/source/d/dagster-dbt/dagster-dbt-${{ version }}.tar.gz
      sha256: aab33c0170a18ecaa0890294e8ede4047d7bc194963a30ca47bce31bb97546eb
    build:
      noarch: python
      script: ${{ PYTHON }} ${{ pip_install }}
      python:
        entry_points:
          - dagster-dbt-cloud = dagster_dbt.cloud.cli:app
          - dagster-dbt = dagster_dbt.cli.app:app
    requirements:
      host:
        - ${{ dagster_python }}
        - pip
        - python ${{ python_min }}.*
        - setuptools
      run:
        - ${{ dagster }}
        - ${{ dagster_dbt_core }}
        - ${{ dagster_python }}
        - jinja2
        - networkx
        - orjson
        - packaging
        - python >=${{ python_min }}
        - requests
        - rich
        - sqlglot-rs
        - typer >=0.9.0
    tests:
      - python:
          pip_check: true
          python_version: ${{ python_min }}
          imports:
            - dagster_dbt
      - script:
          - dagster-dbt-cloud --help
          - dagster-dbt --help
    about:
      homepage: https://github.com/dagster-io/dagster/tree/master/python_modules/dagster-dbt
      license: Apache-2.0
      license_file: LICENSE
      summary: A Dagster integration for dbt

  - package:
      name: dagster-deltalake
    source:
      url: https://pypi.org/packages/source/d/dagster-deltalake/dagster-deltalake-${{ version }}.tar.gz
      sha256: 8cd2e8a664e5ccf96a507e3af706f9d04cb4f564127a662026c3526b394892ae
    build:
      noarch: python
      script: ${{ PYTHON }} ${{ pip_install }}
    requirements:
      host:
        - ${{ dagster_python }}
        - pip
        - python ${{ python_min }}.*
        - setuptools
      run:
        - ${{ dagster }}
        - ${{ dagster_python }}
        - deltalake >=0.25.0
        - python >=${{ python_min }}
    tests:
      - python:
          pip_check: true
          python_version: ${{ python_min }}
          imports:
            - dagster_deltalake
    about:
      homepage: https://github.com/dagster-io/dagster/tree/master/python_modules/dagster-deltalake
      license: Apache-2.0
      license_file: LICENSE
      summary: Package for Deltalake-specific Dagster framework op and resource components.

  - package:
      name: dagster-deltalake-pandas
    source:
      url: https://pypi.org/packages/source/d/dagster-deltalake-pandas/dagster-deltalake-pandas-${{ version }}.tar.gz
      sha256: aea1e7418f26e2bdb192a731e1d0a4b3325ee2d17245a63c29f11649fc4c6cc5
    build:
      noarch: python
      script: ${{ PYTHON }} ${{ pip_install }}
    requirements:
      host:
        - ${{ dagster_python }}
        - pip
        - python ${{ python_min }}.*
        - setuptools
      run:
        - ${{ dagster }}
        - ${{ dagster_python }}
        - ${{ pin_subpackage("dagster-deltalake", upper_bound="x.x.x") }}
        - pandas
        - python >=${{ python_min }}
    tests:
      - python:
          pip_check: true
          python_version: ${{ python_min }}
          imports:
            - dagster_deltalake_pandas
    about:
      homepage: https://github.com/dagster-io/dagster/tree/master/python_modules/dagster-deltalake-pandas
      license: Apache-2.0
      license_file: LICENSE
      summary: Package for storing Pandas DataFrames in Delta tables

  - package:
      name: dagster-deltalake-polars
    source:
      url: https://pypi.org/packages/source/d/dagster-deltalake-polars/dagster-deltalake-polars-${{ version }}.tar.gz
      sha256: 95a72c979d24293384e4ead8bbea1428974d63f9b8ab759448974c5845bfde7f
    build:
      noarch: python
      script: ${{ PYTHON }} ${{ pip_install }}
    requirements:
      host:
        - ${{ dagster_python }}
        - pip
        - python ${{ python_min }}.*
        - setuptools
      run:
        - ${{ dagster }}
        - ${{ dagster_python }}
        - ${{ pin_subpackage("dagster-deltalake", upper_bound="x.x.x") }}
        - polars
        - python >=${{ python_min }}
        # TODO: update after https://github.com/conda-forge/polars-feedstock/issues/194
        # for now https://github.com/pola-rs/polars/blob/main/py-polars/pyproject.toml#L52C13-L52C29
        - pyarrow >=7.0.0
    tests:
      - python:
          pip_check: true
          python_version: ${{ python_min }}
          imports:
            - dagster_deltalake_polars
    about:
      homepage: https://github.com/dagster-io/dagster/tree/master/python_modules/dagster-deltalake-polars
      license: Apache-2.0
      license_file: LICENSE
      summary: Package for storing Polars DataFrames in Delta tables.

  - package:
      name: dagster-docker
    source:
      url: https://pypi.org/packages/source/d/dagster-docker/dagster-docker-${{ version }}.tar.gz
      sha256: 0fa146cad1e1228c3413c6a83f7cf53c93d79cc0936b5b0876037e406af41c4c
    build:
      noarch: python
      script: ${{ PYTHON }} ${{ pip_install }}
    requirements:
      host:
        - ${{ dagster_python }}
        - pip
        - python ${{ python_min }}.*
        - setuptools
      run:
        - ${{ dagster }}
        - ${{ dagster_python }}
        - docker-image-py
        - docker-py
        - python >=${{ python_min }}
    tests:
      - python:
          pip_check: true
          python_version: ${{ python_min }}
          imports:
            - dagster_docker

    about:
      homepage: https://github.com/dagster-io/dagster/tree/master/python_modules/libraries/dagster-docker
      license: Apache-2.0
      license_file: LICENSE
      summary: A Dagster integration for docker


  - package:
      name: dagster-duckdb
    source:
      url: https://pypi.org/packages/source/d/dagster-duckdb/dagster-duckdb-${{ version }}.tar.gz
      sha256: 85e95f751affd0cc431229a0d6b301e69452cd91f47a4e9bf948ebe663f6e426
    build:
      noarch: python
      script: ${{ PYTHON }} ${{ pip_install }}
    requirements:
      host:
        - ${{ dagster_python }}
        - pip
        - python ${{ python_min }}.*
        - setuptools
      run:
        - ${{ dagster }}
        - ${{ dagster_python }}
        - python >=${{ python_min }}
        - python-duckdb <0.10.0
    tests:
      - python:
          pip_check: true
          python_version: ${{ python_min }}
          imports:
            - dagster_duckdb
    about:
      homepage: https://github.com/dagster-io/dagster/blob/master/python_modules/dagster-duckdb
      license: Apache-2.0
      license_file: LICENSE
      summary: Package for DuckDB-specific Dagster framework op and resource components.

  - package:
      name: dagster-duckdb-pandas
    source:
      url: https://pypi.org/packages/source/d/dagster-duckdb-pandas/dagster-duckdb-pandas-${{ version }}.tar.gz
      sha256: 185fb9e205edb456582e98e534b05ae4718afcb6c741af8659715e418a4212f2
    build:
      noarch: python
      script: ${{ PYTHON }} ${{ pip_install }}
    requirements:
      host:
        - ${{ dagster_python }}
        - pip
        - python ${{ python_min }}.*
        - setuptools
      run:
        - ${{ dagster }}
        - ${{ dagster_python }}
        - ${{ pin_subpackage("dagster-duckdb", upper_bound="x.x.x") }}
        - pandas
        - python >=${{ python_min }}
    tests:
      - python:
          pip_check: true
          python_version: ${{ python_min }}
          imports:
            - dagster_duckdb_pandas
    about:
      homepage: https://github.com/dagster-io/dagster/blob/master/python_modules/dagster-duckdb-pandas
      license: Apache-2.0
      license_file: LICENSE
      summary: Package for storing Pandas DataFrames in DuckDB.

  - package:
      name: dagster-duckdb-polars
    source:
      url: https://pypi.org/packages/source/d/dagster-duckdb-polars/dagster-duckdb-polars-${{ version }}.tar.gz
      sha256: 5831892a140a338b9fdf958c37f425aa00488ba24e1aeb049a9e5da43a22d77c
    build:
      noarch: python
      script: ${{ PYTHON }} ${{ pip_install }}
    requirements:
      host:
        - ${{ dagster_python }}
        - pip
        - python ${{ python_min }}.*
        - setuptools
      run:
        - ${{ dagster }}
        - ${{ dagster_python }}
        - ${{ pin_subpackage("dagster-duckdb", upper_bound="x.x.x") }}
        - pyarrow >=4.0.0
        - python >=${{ python_min }}
        # TODO: get polars-with-X outputs
        - polars
    tests:
      - python:
          pip_check: true
          python_version: ${{ python_min }}
          imports:
            - dagster_duckdb_polars
    about:
      homepage: https://github.com/dagster-io/dagster/blob/master/python_modules/dagster-duckdb-polars
      license: Apache-2.0
      license_file: LICENSE
      summary: Package for storing Polars DataFrames in DuckDB.
  - package:
      name: dagster-duckdb-pyspark
    source:
      url: https://pypi.org/packages/source/d/dagster-duckdb-pyspark/dagster-duckdb-pyspark-${{ version }}.tar.gz
      sha256: 87c26403d4f342f973db2b0ca31d082eed674ede4f5e4c241d8408360d16020b
    build:
      noarch: python
      script: ${{ PYTHON }} ${{ pip_install }}
    requirements:
      host:
        - ${{ dagster_python }}
        - pip
        - python ${{ python_min }}.*
        - setuptools
      run:
        - ${{ dagster }}
        - ${{ dagster_python }}
        - ${{ pin_subpackage("dagster-duckdb", upper_bound="x.x.x") }}
        - pandas
        - pyarrow
        - pyspark >=3
        - python >=${{ python_min }}
    tests:
      - python:
          pip_check: true
          python_version: ${{ python_min }}
          imports:
            - dagster_duckdb_pyspark
    about:
      homepage: https://github.com/dagster-io/dagster/blob/master/python_modules/dagster-duckdb-pyspark
      license: Apache-2.0
      license_file: LICENSE
      summary: Package for storing PySpark DataFrames in DuckDB.

  # E
  # F
  - package:
      name: dagster-fivetran
    source:
      url: https://pypi.org/packages/source/d/dagster-fivetran/dagster-fivetran-${{ version }}.tar.gz
      sha256: e2121b1b17efb7b1ee0b8c668b9e746af3312aa9a18a87a7e355b987f39d25e3
    build:
      noarch: python
      script: ${{ PYTHON }} ${{ pip_install }}
      python:
        entry_points:
          - dagster-fivetran = dagster_fivetran.cli:main
    requirements:
      host:
        - ${{ dagster_python }}
        - pip
        - python ${{ python_min }}.*
        - setuptools
      run:
        - ${{ dagster }}
        - ${{ dagster_python }}
        - python >=${{ python_min }}
        - requests
    tests:
      - python:
          pip_check: true
          python_version: ${{ python_min }}
          imports:
            - dagster_fivetran
      - script:
          - dagster-fivetran --help
    about:
      homepage: https://github.com/dagster-io/dagster/tree/master/python_modules/libraries/dagster-fivetran
      license: Apache-2.0
      license_file: LICENSE
      summary: Package for integrating Fivetran with Dagster.

  # G
  - package:
      name: dagster-gcp
    source:
      url: https://pypi.org/packages/source/d/dagster-gcp/dagster-gcp-${{ version }}.tar.gz
      sha256: 6ba970b0abdc64d2bf9893fa7a8dad1af83cf850996ea2cf66e4f71ca664fcbe
    build:
      noarch: python
      script: ${{ PYTHON }} ${{ pip_install }}
    requirements:
      host:
        - ${{ dagster_python }}
        - pip
        - python ${{ python_min }}.*
        - setuptools
      run:
        - ${{ dagster }}
        - ${{ dagster_python }}
        - ${{ pin_subpackage("dagster-pandas", upper_bound="x.x.x") }}
        - db-dtypes
        - google-api-python-client
        - google-cloud-bigquery-core
        - google-cloud-storage
        - oauth2client
        - python >=${{ python_min }}
    tests:
      - python:
          pip_check: true
          python_version: ${{ python_min }}
          imports:
            - dagster_gcp
            - dagster_gcp.bigquery
            - dagster_gcp.dataproc
            - dagster_gcp.gcs
    about:
      homepage: https://github.com/dagster-io/dagster/tree/master/python_modules/libraries/dagster-gcp
      license: Apache-2.0
      license_file: LICENSE
      summary: Package for GCP-specific Dagster framework solid and resource components.

  - package:
      name: dagster-gcp-pandas
    source:
      url: https://pypi.org/packages/source/d/dagster-gcp-pandas/dagster-gcp-pandas-${{ version }}.tar.gz
      sha256: d4963e9ba6bf57da6b1d886a465e92fb2596b09ea33cd039276d037fbe18e0de
    build:
      noarch: python
      script: ${{ PYTHON }} ${{ pip_install }}
    requirements:
      host:
        - ${{ dagster_python }}
        - pip
        - python ${{ python_min }}.*
        - setuptools
      run:
        - ${{ dagster }}
        - ${{ dagster_python }}
        - ${{ pin_subpackage("dagster-gcp", upper_bound="x.x.x") }}
        - pandas
        - python >=${{ python_min }}
    tests:
      - python:
          pip_check: true
          python_version: ${{ python_min }}
          imports:
            - dagster_gcp_pandas
    about:
      homepage: https://github.com/dagster-io/dagster/tree/master/python_modules/libraries/dagster-gcp-pandas
      license: Apache-2.0
      license_file: LICENSE
      summary: Package for storing Pandas DataFrames in GCP.

  - package:
      name: dagster-gcp-pyspark
    source:
      url: https://pypi.org/packages/source/d/dagster-gcp-pyspark/dagster-gcp-pyspark-${{ version }}.tar.gz
      sha256: 8a48adce419cbdde82bfd5b8d27b5ae796caafcb2c670113229b6da354b2b3f6
    build:
      noarch: python
      script: ${{ PYTHON }} ${{ pip_install }}
    requirements:
      host:
        - ${{ dagster_python }}
        - pip
        - python ${{ python_min }}.*
        - setuptools
      run:
        - ${{ dagster }}
        - ${{ dagster_python }}
        - ${{ pin_subpackage("dagster-gcp", upper_bound="x.x.x") }}
        - pyspark
        - python >=${{ python_min }}
    tests:
      - python:
          pip_check: true
          python_version: ${{ python_min }}
          imports:
            - dagster_gcp_pyspark
    about:
      homepage: https://github.com/dagster-io/dagster/tree/master/python_modules/libraries/dagster-gcp-pyspark
      license: Apache-2.0
      license_file: LICENSE
      summary: Package for storing PySpark DataFrames in GCP

  - package:
      name: dagster-ge
    source:
      url: https://pypi.org/packages/source/d/dagster-ge/dagster-ge-${{ version }}.tar.gz
      sha256: 220e850b736da5697c44907876a027a7a748bb7273d0193ccf7364c34ca48eda
    build:
      noarch: python
      script: ${{ PYTHON }} ${{ pip_install }}
    requirements:
      host:
        - ${{ dagster_ge_python }}
        - ${{ dagster_python }}
        - pip
        - setuptools
        # must be 3.10
        # - python >=${{ python_min }}
      run:
        - ${{ dagster }}
        - ${{ dagster_ge_python }}
        - ${{ dagster_python }}
        - ${{ pin_subpackage("dagster-pandas", upper_bound="x.x.x") }}
        - dataclasses
        - great-expectations >=0.17.15,<1.0.0
        # must be 3.10
        # - python >=${{ python_min }}
    tests:
      - python:
          pip_check: true
          python_version: '3.10'
          imports:
            - dagster_ge
    about:
      homepage: https://github.com/dagster-io/dagster/tree/master/python_modules/libraries/dagster-ge
      license: Apache-2.0
      license_file: LICENSE
      summary: Package for GE-specific Dagster framework solid and resource components.

  - package:
      name: dagster-github
    source:
      url: https://pypi.org/packages/source/d/dagster-github/dagster-github-${{ version }}.tar.gz
      sha256: 2d3b8482812ff1f4f88e876d3b51c0029b7ec1cc98be1c026db19c0f9bfdbcb8
    build:
      noarch: python
      script: ${{ PYTHON }} ${{ pip_install }}
    requirements:
      host:
        - ${{ dagster_python }}
        - pip
        - python ${{ python_min }}.*
        - setuptools
      run:
        - ${{ dagster }}
        - ${{ dagster_python }}
        - cryptography >=1.4
        - pyjwt
        - python >=${{ python_min }}
        - requests
    tests:
      - python:
          pip_check: true
          python_version: ${{ python_min }}
          imports:
            - dagster_github
    about:
      homepage: https://github.com/dagster-io/dagster/tree/master/python_modules/libraries/dagster-github
      license: Apache-2.0
      license_file: LICENSE
      summary: Github plugin for Dagster

  # H
  # I
  # J
  # K
  - package:
      name: dagster-k8s
    source:
      url: https://pypi.org/packages/source/d/dagster-k8s/dagster-k8s-${{ version }}.tar.gz
      sha256: 59afcc0c55c5df1b54fe10472894ecd581694b8f5143d8a4c2c793edb99d104b
    build:
      noarch: python
      script: ${{ PYTHON }} ${{ pip_install }}
    requirements:
      host:
        - ${{ dagster_python }}
        - pip
        - python ${{ python_min }}.*
        - setuptools
      run:
        - ${{ dagster }}
        - ${{ dagster_python }}
        - google-auth !=2.23.1
        - python >=${{ python_min }}
        # from python_modules/libraries/dagster-k8s/dagster_k8s/kubernetes_version.py
        - python-kubernetes <33
    tests:
      - python:
          pip_check: true
          python_version: ${{ python_min }}
          imports:
            - dagster_k8s
    about:
      homepage: https://github.com/dagster-io/dagster/tree/master/python_modules/libraries/dagster-k8s
      license: Apache-2.0
      license_file: LICENSE
      summary: A Dagster integration for k8s

  # L
  - package:
      name: dagster-looker
    source:
      url: https://pypi.org/packages/source/d/dagster-looker/dagster_looker-${{ version }}.tar.gz
      sha256: d8ec9e29194e5d19b0e9fbdfed0eb84ea0da95f5af16fb521784246fded7fc3e
    build:
      noarch: python
      script: ${{ PYTHON }} ${{ pip_install }}
    requirements:
      host:
        - ${{ dagster_python }}
        - pip
        - python ${{ python_min }}.*
        - setuptools
      run:
        - ${{ dagster }}
        - ${{ dagster_python }}
        - cattrs <23.2
        - lkml
        - looker-sdk <24.18.0
        - python >=${{ python_min }}
        - python-liquid <2
        - sqlglot >=25.19.0
    tests:
      - python:
          pip_check: true
          python_version: ${{ python_min }}
          imports:
            - dagster_looker
    about:
      homepage: https://github.com/dagster-io/dagster/tree/master/python_modules/libraries/dagster-looker
      license: Apache-2.0
      license_file: LICENSE
      summary: Looker with Dagster.

  # M
  - package:
      name: dagster-managed-elements
    source:
      url: https://pypi.org/packages/source/d/dagster-managed-elements/dagster-managed-elements-${{ version }}.tar.gz
      sha256: 5d05df96b7be6047ee373967ab31e376e7463ba073dde451daa81485289199b6
    build:
      noarch: python
      script: ${{ PYTHON }} ${{ pip_install }}
      python:
        entry_points:
          - dagster-managed-elements = dagster_managed_elements.cli:main
          - dagster-me = dagster_managed_elements.cli:main
    requirements:
      host:
        - ${{ dagster_python }}
        - pip
        - python ${{ python_min }}.*
        - setuptools
      run:
        - ${{ dagster }}
        - ${{ dagster_python }}
        - click-spinner
        - python >=${{ python_min }}
        - requests
    tests:
      - python:
          pip_check: true
          python_version: ${{ python_min }}
          imports:
            - dagster_managed_elements
      - script:
          - dagster-managed-elements --help
          - dagster-me --help
    about:
      homepage: https://github.com/dagster-io/dagster/tree/master/python_modules/libraries/dagster-managed-elements
      license: Apache-2.0
      license_file: LICENSE
      summary: Package for Managed elements with Dagster.

  - package:
      name: dagster-mlflow
    source:
      url: https://pypi.org/packages/source/d/dagster-mlflow/dagster-mlflow-${{ version }}.tar.gz
      sha256: b12dea28245c58569ef74b033dd74a27d2eb6e316174e4e65c8df25436951320
    build:
      noarch: python
      script: ${{ PYTHON }} ${{ pip_install }}
    requirements:
      host:
        - ${{ dagster_python }}
        - pip
        - python ${{ python_min }}.*
        - setuptools
      run:
        - ${{ dagster }}
        - ${{ dagster_python }}
        - mlflow
        - pandas
        - python >=${{ python_min }}
        # https://github.com/protocolbuffers/protobuf/issues/19430
        - protobuf !=5.29.0
    tests:
      - python:
          # as of #60, see `dagster-mlflow X requires mlflow, which is not installed``
          # due to some "clever" packaging tricks with mlflow(-skinny)
          # pip_check: true
          python_version: ${{ python_min }}
          imports:
            - dagster_mlflow
    about:
      homepage: https://github.com/dagster-io/dagster/tree/master/python_modules/libraries/dagster-mlflow
      license: Apache-2.0
      license_file: LICENSE
      summary: Package for mlflow Dagster framework components

  - package:
      name: dagster-msteams
    source:
      url: https://pypi.org/packages/source/d/dagster-msteams/dagster-msteams-${{ version }}.tar.gz
      sha256: 4a0fd7309375015e8d8ac4a2479b58b1e484204a5759aeaf344af199212ead3b
    build:
      noarch: python
      script: ${{ PYTHON }} ${{ pip_install }}
    requirements:
      host:
        - ${{ dagster_python }}
        - pip
        - python ${{ python_min }}.*
        - setuptools
      run:
        - ${{ dagster }}
        - ${{ dagster_python }}
        - python >=${{ python_min }}
        - requests >=2,<3
    tests:
      - python:
          pip_check: true
          python_version: ${{ python_min }}
          imports:
            - dagster_msteams
    about:
      homepage: https://github.com/dagster-io/dagster/tree/master/python_modules/libraries/dagster-msteams
      license: Apache-2.0
      license_file: LICENSE
      summary: A Microsoft Teams client resource for posting to Microsoft Teams

  - package:
      name: dagster-mysql
    source:
      url: https://pypi.org/packages/source/d/dagster-mysql/dagster-mysql-${{ version }}.tar.gz
      sha256: a994424216ad5af0337fea4801fb8b75194e0dcb983c85a8a584d6bfdb522493
    build:
      noarch: python
      script: ${{ PYTHON }} ${{ pip_install }}
    requirements:
      host:
        - ${{ dagster_python }}
        - pip
        - python ${{ python_min }}.*
        - setuptools
      run:
        - ${{ dagster }}
        - ${{ dagster_python }}
        - mysql-connector-python
        - python >=${{ python_min }}
    tests:
      - python:
          pip_check: true
          python_version: ${{ python_min }}
          imports:
            - dagster_mysql
    about:
      homepage: https://github.com/dagster-io/dagster/tree/master/python_modules/libraries/dagster-mysql
      license: Apache-2.0
      license_file: LICENSE
      summary: A Dagster integration for MySQL

  # N
  # O
  - package:
      name: dagster-openai
    source:
      url: https://pypi.org/packages/source/d/dagster-openai/dagster-openai-${{ version }}.tar.gz
      sha256: 3a4fd39397ac1f5f790d759be64dcf0b81048f56592fbd72c185f5a1a4057d1f
    build:
      noarch: python
      script: ${{ PYTHON }} ${{ pip_install }}
    requirements:
      host:
        - ${{ dagster_python }}
        - pip
        - python ${{ python_min }}.*
        - setuptools
      run:
        - ${{ dagster }}
        - ${{ dagster_python }}
        - openai
        - python >=${{ python_min }}
    tests:
      - python:
          pip_check: true
          python_version: ${{ python_min }}
          imports:
            - dagster_openai
    about:
      homepage: https://github.com/dagster-io/dagster/tree/master/python_modules/libraries/dagster-openai
      license: Apache-2.0
      license_file: LICENSE
      summary: A Open AI client resource for interacting with Open AI API

  # P
  - package:
      name: dagster-pagerduty
    source:
      url: https://pypi.org/packages/source/d/dagster-pagerduty/dagster-pagerduty-${{ version }}.tar.gz
      sha256: a52ef0a795207c90705a66aaffa7bd62a998e8c0a2be804fda300e3822d53d11
    build:
      noarch: python
      script: ${{ PYTHON }} ${{ pip_install }}
    requirements:
      host:
        - ${{ dagster_python }}
        - pip
        - python ${{ python_min }}.*
        - setuptools
      run:
        - ${{ dagster }}
        - ${{ dagster_python }}
        - pypd
        - python >=${{ python_min }}
    tests:
      - python:
          # │ │ + pip check
          # │ │ pypd 1.1.0 is not supported on this platform
          pip_check: false
          python_version: ${{ python_min }}
          imports:
            - dagster_pagerduty
    about:
      homepage: https://github.com/dagster-io/dagster/tree/master/python_modules/libraries/dagster-pagerduty
      license: Apache-2.0
      license_file: LICENSE
      summary: Package for pagerduty Dagster framework components.

  - package:
      name: dagster-pandas
    source:
      url: https://pypi.org/packages/source/d/dagster-pandas/dagster-pandas-${{ version }}.tar.gz
      sha256: 28a6809cf24bd9be84db40a4490ac5a5a7134fc0337bf6d444f3d4394c1bcc6c
    build:
      noarch: python
      script: ${{ PYTHON }} ${{ pip_install }}
    requirements:
      host:
        - ${{ dagster_python }}
        - pip
        - python ${{ python_min }}.*
        - setuptools
      run:
        - ${{ dagster }}
        - ${{ dagster_python }}
        - pandas
        - python >=${{ python_min }}
    tests:
      - python:
          pip_check: true
          python_version: ${{ python_min }}
          imports:
            - dagster_pandas
    about:
      homepage: https://github.com/dagster-io/dagster
      license: Apache-2.0
      license_file: LICENSE
      summary: Utilities and examples for working with pandas and dagster, an opinionated framework for expressing data pipelines

  - package:
      name: dagster-pandera
    source:
      url: https://pypi.org/packages/source/d/dagster-pandera/dagster-pandera-${{ version }}.tar.gz
      sha256: 44845cc71960f0c57cb310f172bae1b0d02f69a706cec3009140d47d82d84336
    build:
      noarch: python
      script: ${{ PYTHON }} ${{ pip_install }}
    requirements:
      host:
        - ${{ dagster_python }}
        - pip
        - python ${{ python_min }}.*
        - setuptools
      run:
        - ${{ dagster }}
        - ${{ dagster_python }}
        - pandas
        - pandera >=0.15.0
        - python >=${{ python_min }}
    tests:
      - python:
          pip_check: true
          python_version: ${{ python_min }}
          imports:
            - dagster_pandera
    about:
      homepage: https://github.com/dagster-io/dagster/tree/master/python_modules/libraries/dagster-pandera
      license: Apache-2.0
      license_file: LICENSE
      summary: Integration layer for dagster and pandera.


  - package:
      name: dagster-papertrail
    source:
      url: https://pypi.org/packages/source/d/dagster-papertrail/dagster-papertrail-${{ version }}.tar.gz
      sha256: f598d0a44f2b25f31b6d1a5eb9e4ece2fde168768fbe9dfccab60e35cbdc59b4
    build:
      noarch: python
      script: ${{ PYTHON }} ${{ pip_install }}
    requirements:
      host:
        - ${{ dagster_python }}
        - pip
        - python ${{ python_min }}.*
        - setuptools
      run:
        - ${{ dagster }}
        - ${{ dagster_python }}
        - python >=${{ python_min }}
    tests:
      - python:
          pip_check: true
          python_version: ${{ python_min }}
          imports:
            - dagster_papertrail
    about:
      homepage: https://github.com/dagster-io/dagster/tree/master/python_modules/libraries/dagster-papertrail
      license: Apache-2.0
      license_file: LICENSE
      summary: Package for papertrail Dagster framework components.

  - package:
      name: dagster-polars
    source:
      url: https://pypi.org/packages/source/d/dagster-polars/dagster-polars-${{ version }}.tar.gz
      sha256: 92ae4020fd636f6e506345409ccf855085a16ef2863ba768a79bd191b41c22db
    build:
      noarch: python
      script: ${{ PYTHON }} ${{ pip_install }}
    requirements:
      host:
        - ${{ dagster_python }}
        - pip
        - python ${{ python_min }}.*
        - setuptools
      run:
        - ${{ dagster }}
        - ${{ dagster_python }}
        - polars >=0.20.0
        - pyarrow >=8.0.0
        - python >=${{ python_min }}
        - typing_extensions >=4.7.0
        - universal_pathlib >=0.1.4
    tests:
      - python:
          pip_check: true
          python_version: ${{ python_min }}
          imports:
            - dagster_polars
    about:
      homepage: https://github.com/dagster-io/dagster/tree/master/python_modules/libraries/dagster-polars
      license: Apache-2.0
      license_file: LICENSE
      summary: A Dagster integration for polars

  - package:
      name: dagster-postgres
    source:
      url: https://pypi.org/packages/source/d/dagster-postgres/dagster-postgres-${{ version }}.tar.gz
      sha256: 94dc9744570c3ce93ee3e3ffd7ded168ea26c42bcf584d6798c032e6086f72fe
    build:
      noarch: python
      script: ${{ PYTHON }} ${{ pip_install }}
    requirements:
      host:
        - ${{ dagster_python }}
        - pip
        - python ${{ python_min }}.*
        - setuptools
      run:
        - ${{ dagster }}
        - ${{ dagster_python }}
        - psycopg2-binary
        - python >=${{ python_min }}
    tests:
      - python:
          pip_check: true
          python_version: ${{ python_min }}
          imports:
            - dagster_postgres
    about:
      homepage: https://github.com/dagster-io/dagster/tree/master/python_modules/libraries/dagster-postgres
      license: Apache-2.0
      license_file: LICENSE
      summary: A Dagster integration for postgres

  - package:
      name: dagster-powerbi
    source:
      url: https://pypi.org/packages/source/d/dagster-powerbi/dagster_powerbi-${{ version }}.tar.gz
      sha256: 3e70850bc8c19ddcbaf0cd29f0c67947769569e7b9b586a965760a4bd9576194
    build:
      noarch: python
      script: ${{ PYTHON }} ${{ pip_install }}
    requirements:
      host:
        - ${{ dagster_python }}
        - pip
        - python ${{ python_min }}.*
        - setuptools
      run:
        - ${{ dagster }}
        - ${{ dagster_python }}
        - python >=${{ python_min }}
    tests:
      - python:
          pip_check: true
          python_version: ${{ python_min }}
          imports:
            - dagster_powerbi
    about:
      homepage: https://github.com/dagster-io/dagster/tree/master/python_modules/libraries/dagster-powerbi
      license: Apache-2.0
      license_file: LICENSE
      summary: Build assets representing Power BI dashboards and reports.

  - package:
      name: dagster-prometheus
    source:
      url: https://pypi.org/packages/source/d/dagster-prometheus/dagster-prometheus-${{ version }}.tar.gz
      sha256: 67dfbded64bc4b36e3799b6d923cb4a6c1e365fed87cf012ac500b8f1799b119
    build:
      noarch: python
      script: ${{ PYTHON }} ${{ pip_install }}
    requirements:
      host:
        - ${{ dagster_python }}
        - pip
        - python ${{ python_min }}.*
        - setuptools
      run:
        - ${{ dagster }}
        - ${{ dagster_python }}
        - prometheus_client
        - python >=${{ python_min }}
    tests:
      - python:
          pip_check: true
          python_version: ${{ python_min }}
          imports:
            - dagster_prometheus
    about:
      homepage: https://github.com/dagster-io/dagster/tree/master/python_modules/libraries/dagster-prometheus
      license: Apache-2.0
      license_file: LICENSE
      summary: A Dagster integration for prometheus

  - package:
      name: dagster-pyspark
    source:
      url: https://pypi.org/packages/source/d/dagster-pyspark/dagster-pyspark-${{ version }}.tar.gz
      sha256: f5a62c4610d85c6c72aefb15f7c605e11af44248b25f59f0ae0ea3a58a99f41d
    build:
      noarch: python
      script: ${{ PYTHON }} ${{ pip_install }}
    requirements:
      host:
        - ${{ dagster_python }}
        - pip
        - python ${{ python_min }}.*
        - setuptools
      run:
        - ${{ dagster }}
        - ${{ dagster_python }}
        - ${{ pin_subpackage("dagster-spark", upper_bound="x.x.x") }}
        - pyspark >=3
        - python >=${{ python_min }}
    tests:
      - python:
          pip_check: true
          python_version: ${{ python_min }}
          imports:
            - dagster_pyspark
    about:
      homepage: https://github.com/dagster-io/dagster/tree/master/python_modules/libraries/dagster-pyspark
      license: Apache-2.0
      license_file: LICENSE
      summary: Package for PySpark Dagster framework components.

  # Q
  # R
  # S
  - package:
      name: dagster-sigma
    source:
      url: https://pypi.org/packages/source/d/dagster-sigma/dagster_sigma-${{ version }}.tar.gz
      sha256: 49a6fcc88f09c9366954102d8afd404659201450a9592cca2c69192606272eb1
    build:
      noarch: python
      script: ${{ PYTHON }} ${{ pip_install }}
      python:
        entry_points:
          - dagster-sigma = dagster_sigma.cli:app
    requirements:
      host:
        - ${{ dagster_python }}
        - pip
        - python ${{ python_min }}.*
        - setuptools
      run:
        - ${{ dagster }}
        - ${{ dagster_python }}
        - aiohttp
        - python >=${{ python_min }}
        - sqlglot
    tests:
      - python:
          pip_check: true
          python_version: ${{ python_min }}
          imports:
            - dagster_sigma
      - script:
          - which dagster-sigma || where dagster-sigma
    about:
      homepage: https://github.com/dagster-io/dagster/tree/master/python_modules/libraries/dagster-sigma
      license: Apache-2.0
      license_file: LICENSE
      summary: Build assets representing Sigma dashboards.

  - package:
      name: dagster-slack
    source:
      url: https://pypi.org/packages/source/d/dagster-slack/dagster-slack-${{ version }}.tar.gz
      sha256: 8fdbbc764c3b7617c43f0f615f6abd3daa45b72d4374dd7c9436330f1dbbb463
    build:
      noarch: python
      script: ${{ PYTHON }} ${{ pip_install }}
    requirements:
      host:
        - ${{ dagster_python }}
        - pip
        - python ${{ python_min }}.*
        - setuptools
      run:
        - ${{ dagster }}
        - ${{ dagster_python }}
        - python >=${{ python_min }}
        - slack-sdk
    tests:
      - python:
          pip_check: true
          python_version: ${{ python_min }}
          imports:
            - dagster_slack
    about:
      homepage: https://github.com/dagster-io/dagster/tree/master/python_modules/libraries/dagster-slack
      license: Apache-2.0
      license_file: LICENSE
      summary: A Slack client resource for posting to Slack

  - package:
      name: dagster-snowflake
    source:
      url: https://pypi.org/packages/source/d/dagster-snowflake/dagster-snowflake-${{ version }}.tar.gz
      sha256: 63af0dd87d240ebdfd85cac0a4e55985f186789a376450e969fab5ef7a32ce92
    build:
      noarch: python
      script: ${{ PYTHON }} ${{ pip_install }}
    requirements:
      host:
        - ${{ dagster_python }}
        - pip
        - python ${{ python_min }}.*
        - setuptools
      run:
        - ${{ dagster }}
        - ${{ dagster_python }}
        - pyopenssl >=22.1.0
        - python >=${{ python_min }}
        - snowflake-connector-python >=3.4.0
    tests:
      - python:
          pip_check: true
          python_version: ${{ python_min }}
          imports:
            - dagster_snowflake
    about:
      homepage: https://github.com/dagster-io/dagster/tree/master/python_modules/libraries/dagster-snowflake
      license: Apache-2.0
      license_file: LICENSE
      summary: Package for Snowflake Dagster framework components.

  - package:
      name: dagster-spark
    source:
      url: https://pypi.org/packages/source/d/dagster-spark/dagster-spark-${{ version }}.tar.gz
      sha256: b45002cb686bfd115f0de378f0e3a0a94adbe907e620da1a51e81e706726d547
    build:
      noarch: python
      script: ${{ PYTHON }} ${{ pip_install }}
    requirements:
      host:
        - ${{ dagster_python }}
        - pip
        - python ${{ python_min }}.*
        - setuptools
      run:
        - ${{ dagster }}
        - ${{ dagster_python }}
        - python >=${{ python_min }}
    tests:
      - python:
          pip_check: true
          python_version: ${{ python_min }}
          imports:
            - dagster_spark
    about:
      homepage: https://github.com/dagster-io/dagster/tree/master/python_modules/libraries/dagster-spark
      license: Apache-2.0
      license_file: LICENSE
      summary: Package for Spark Dagster framework components.

  - package:
      name: dagster-snowflake-pandas
    source:
      url: https://pypi.org/packages/source/d/dagster-snowflake-pandas/dagster-snowflake-pandas-${{ version }}.tar.gz
      sha256: 8eeb22f9294104c6efee490f44eab6ec5ea02cdc29c913ff563f31738c114e59
    build:
      noarch: python
      script: ${{ PYTHON }} ${{ pip_install }}
    requirements:
      host:
        - ${{ dagster_python }}
        - pip
        - python ${{ python_min }}.*
        - setuptools
      run:
        - ${{ dagster }}
        - ${{ dagster_python }}
        - ${{ pin_subpackage("dagster-snowflake", upper_bound="x.x.x") }}
        - pandas
        - python >=${{ python_min }}
        - requests
        - snowflake-connector-python >=2.1.0
        - snowflake-sqlalchemy >=1.2
        - sqlalchemy !=1.4.42
    tests:
      - python:
          pip_check: true
          python_version: ${{ python_min }}
          imports:
            - dagster_snowflake_pandas
    about:
      homepage: https://github.com/dagster-io/dagster/tree/master/python_modules/libraries/dagster-snowflake-pandas
      license: Apache-2.0
      license_file: LICENSE
      summary: Package for integrating Snowflake and Pandas with Dagster.

  - package:
      name: dagster-snowflake-pyspark
    source:
      url: https://pypi.org/packages/source/d/dagster-snowflake-pyspark/dagster-snowflake-pyspark-${{ version }}.tar.gz
      sha256: 1bc3f54649ab45e3849ed37e05572fd7a43883a2307970cd6e7639172ea1252e
    build:
      noarch: python
      script: ${{ PYTHON }} ${{ pip_install }}
    requirements:
      host:
        - ${{ dagster_python }}
        - pip
        - python ${{ python_min }}.*
        - setuptools
      run:
        - ${{ dagster }}
        - ${{ dagster_python }}
        - ${{ pin_subpackage("dagster-snowflake", upper_bound="x.x.x") }}
        - pyspark
        - python >=${{ python_min }}
        - requests
        - snowflake-sqlalchemy
    tests:
      - python:
          pip_check: true
          python_version: ${{ python_min }}
          imports:
            - dagster_snowflake_pyspark
    about:
      homepage: https://github.com/dagster-io/dagster/tree/master/python_modules/libraries/dagster-snowflake-pyspark
      license: Apache-2.0
      license_file: LICENSE
      summary: Package for integrating Snowflake and PySpark with Dagster.

  - package:
      name: dagster-ssh
    source:
      url: https://pypi.org/packages/source/d/dagster-ssh/dagster-ssh-${{ version }}.tar.gz
      sha256: 0b884a4543420c8a4adc69ec27897d3e195a5376556f00511774c03c7fedb276
    build:
      noarch: python
      script: ${{ PYTHON }} ${{ pip_install }}
    requirements:
      host:
        - ${{ dagster_python }}
        - pip
        - python ${{ python_min }}.*
        - setuptools
      run:
        - ${{ dagster }}
        - ${{ dagster_python }}
        - paramiko
        - python >=${{ python_min }}
        - sshtunnel
    tests:
      - python:
          pip_check: true
          python_version: ${{ python_min }}
          imports:
            - dagster_ssh
    about:
      homepage: https://github.com/dagster-io/dagster/tree/master/python_modules/libraries/dagster-ssh
      license: Apache-2.0
      license_file: LICENSE
      summary: Package for ssh Dagster framework components.

  # T
  - package:
      name: dagster-tableau
    source:
      url: https://pypi.org/packages/source/d/dagster-tableau/dagster_tableau-${{ version }}.tar.gz
      sha256: 94482a377a1a01b3637bc16d4e016749569db2dda256567a09515d072bc91a0e
    build:
      noarch: python
      script: ${{ PYTHON }} ${{ pip_install }}
    requirements:
      host:
        - ${{ dagster_python }}
        - pip
        - python ${{ python_min }}.*
        - setuptools
      run:
        - ${{ dagster }}
        - ${{ dagster_python }}
        - pyjwt
        - python >=${{ python_min }}
        - tableauserverclient >=0.32
    tests:
      - python:
          pip_check: true
          python_version: ${{ python_min }}
          imports:
            - dagster_tableau
    about:
      homepage: https://github.com/dagster-io/dagster/tree/master/python_modules/libraries/dagster-tableau
      license: Apache-2.0
      license_file: LICENSE
      summary: Build assets representing Tableau workbooks and sheets.

  - package:
      name: dagster-twilio
    source:
      url: https://pypi.org/packages/source/d/dagster-twilio/dagster-twilio-${{ version }}.tar.gz
      sha256: 3ce80c0447963158743a6ece45144aa7ad5f8acb08d80246a74ac7e093ace23d
    build:
      noarch: python
      script: ${{ PYTHON }} ${{ pip_install }}
    requirements:
      host:
        - ${{ dagster_python }}
        - pip
        - python ${{ python_min }}.*
        - setuptools
      run:
        - ${{ dagster }}
        - ${{ dagster_python }}
        - python >=${{ python_min }}
        - twilio
    tests:
      - python:
          pip_check: true
          python_version: ${{ python_min }}
          imports:
            - dagster_twilio
    about:
      homepage: https://github.com/dagster-io/dagster/tree/master/python_modules/libraries/dagster-twilio
      license: Apache-2.0
      license_file: LICENSE
      summary: A Dagster integration for twilio

  # U
  # V
  # W
  - package:
      name: dagster-wandb
    source:
      url: https://pypi.org/packages/source/d/dagster-wandb/dagster-wandb-${{ version }}.tar.gz
      sha256: 2b694148571e0528bf2b1bb07e997aaa7aa7bb7ca7aa0e3e9e12851702adf884
    build:
      noarch: python
      script: ${{ PYTHON }} ${{ pip_install }}
    requirements:
      host:
        - ${{ dagster_python }}
        - pip
        - python ${{ python_min }}.*
        - setuptools
      run:
        - ${{ dagster }}
        - ${{ dagster_python }}
        - python >=${{ python_min }}
        - wandb >=0.15.11,<1.0
    tests:
      - python:
          pip_check: true
          python_version: ${{ python_min }}
          imports:
            - dagster_wandb
    about:
      homepage: https://github.com/dagster-io/dagster/tree/master/python_modules/dagster-wandb
      license: Apache-2.0
      license_file: LICENSE
      summary: Package for wandb Dagster components.

  # X
  # Y
  # Z

  # not hyphenated

  - package:
      name: dagstermill
    source:
      url: https://pypi.org/packages/source/d/dagstermill/dagstermill-${{ version }}.tar.gz
      sha256: 9cfeb608cc35145d5fbffe3624a276e8ff81bd00b96af2f2ee855e266f915633
    build:
      noarch: python
      script: ${{ PYTHON }} ${{ pip_install }}
      python:
        entry_points:
          - dagstermill = dagstermill.cli:main
    requirements:
      host:
        - ${{ dagster_python }}
        - pip
        - python ${{ python_min }}.*
        - setuptools
      run:
        - ${{ dagster }}
        - ${{ dagster_python }}
        - ipykernel >=4.9.0,!=5.4.0,!=5.4.1
        - ipython_genutils >=0.2.0
        - jupyter_client <8
        - nbconvert
        - packaging >=20.9
        - papermill >=1.0.0
        - python >=${{ python_min }}
        - scrapbook >=0.5
    tests:
      - python:
          pip_check: true
          python_version: ${{ python_min }}
          imports:
            - dagstermill
      - script:
          - dagstermill --help
    about:
      homepage: https://github.com/dagster-io/dagster/tree/master/python_modules/dagstermill
      license: Apache-2.0
      license_file: LICENSE
      summary: A Dagster integration for papermill


about:
  homepage: https://github.com/dagster-io
  summary: The data orchestration platform built for productivity.
  license: Apache-2.0
  license_file: LICENSE
  documentation: https://docs.dagster.io
  description: >-
    Dagster is a system for building modern data applications. Combining an elegant programming model and beautiful tools, Dagster allows infrastructure engineers, data engineers, and data scientists to seamlessly collaborate to process and produce the trusted, reliable data needed in today's world.

extra:
  feedstock-name: dagster-libs
  recipe-maintainers:
    - bollwyvl
    - conda-forge/dagster
